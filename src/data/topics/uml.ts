import { Topic } from '../../types';

export const umlTopic: Topic = {
  id: 'uml-modeling',
  title: 'UML统一建模语言全景精讲',
  badge: '建模标准',
  icon: 'TableProperties',
  description: 'M0-M3四层元模型、10大UML图题眼、用例图构造型与虚实箭头、顺序图生命线/激活期/消息符号全解、活动图泳道、类间关系',
  subTopics: [
    {
      id: 'uml-meta-levels',
      title: '1. UML 四层元模型体系结构（M0 ~ M3）',
      tag: '理论深度',
      highlightedKeywords: ['M0', 'M1', 'M2', 'M3', '元元模型', '元模型', '模型层', '用户对象层'],
      content: `### UML 四层体系结构架构及映射

OMG 组织制定的统一建模语言（UML）采用严格的 4 层元模型架构：

| 层次编号 | 层次名称 | 中文解释 | 实际对应内容 |
| :--- | :--- | :--- | :--- |
| **M3** | **元元模型层 (Meta-metamodel)** | 最顶层抽象基础设施，用于定义定义元模型的语言 | MOF (Meta-Object Facility)，定义了 Class、Attribute 等元概念 |
| **M2** | **元模型层 (Metamodel)** | UML 规范本身所处的层，用于定义 UML 模型元素 | UML 元模型本身（如 Class、Association、State 等规范） |
| **M1** | **模型层 (Model)** | 软件设计人员日常画的 UML 图所在的层 | 开发者设计的《教务管理系统类图》、《订单活动图》 |
| **M0** | **用户模型层 // 用户对象层 / 实例层 (User Objects)** | 真实物理世界中运行时的具体实例和数据 | 内存中具体的一个 Student 实例对象（如 \`张三, 2023001\`） |

#### 考题秒杀法则
- 提问“**软件工程师日常绘制的类图、时序图属于哪一层？**” → **M1 层（模型层）**。
- 提问“**定义 UML 本身语言语法的层属于哪一层？**” → **M2 层（元模型层）**。`
    },
    {
      id: 'ten-uml-diagrams',
      title: '2. DBAS各阶段UML图体系与表达方法（官方教材权威版）',
      tag: '教材原版体系',
      highlightedKeywords: [
        '活动图',
        '用例图',
        '系统结构与类图',
        '顺序图',
        '通信图',
        '对象图',
        '状态机图',
        '时间图',
        '包图',
        '交互概述图',
        '复合结构图',
        '组件图',
        '部署图',
        '空心菱形',
        '实心菱形',
        '空心三角'
      ],
      content: `### DBAS 各设计阶段 UML 图全景体系

---

#### 一、业务流程与需求建模

##### 1. 业务流程与活动图
- **活动图**主要描述系统、用例和程序模块中逻辑路径的**执行次序，并行次序**
- **最适合描述系统或子系统的工作流程**
- 活动图用于低层次程序模块的作用**类似于流程图**，但**活动图可以描述并行操作，流程图只能描述串行操作**
- 一张活动图**有且只能有一个起点，可以有多个结束点**

##### 2. 系统需求与用例图
- **系统需求**：用户心中的真正期望
- **用例模型**是把用户需求的所有功能表示出来的工具
- **用例模型由用例、角色、系统5部分组成**：
  - **系统**：各种用例的“黑匣子”
  - **角色**：与系统交互的人或其他实体
  - **用例**：完整功能所有动作集合

##### 3. 角色之间的关系
- **通用化关系**：指把某些角色的行为抽取出来作为通用行为，这些通用行为构成超类

##### 4. 用例与角色之间的关系
- **连接关系**：表明哪种用户可以与该用例通信，是**双向的一对一关系**

##### 5. 用例之间的关系
- **扩展**：一个用例增加新内容成为另一个用例
- **包含（使用）**：一个用例使用另一个用例
- **关联（组合）**：把相关用例打包当作一个整体

---

#### 二、DBAS 系统内部结构表达方法

##### 1. 系统结构与类图
- 系统内部结构一般分为**静态结构**和**动态结构**：
  - 在 UML 中，用**类图**来描述系统**静态结构**
  - 用**顺序图和通信图**来表示系统**动态结构**
- 类图属于概念模型，由**类名、属性、操作（动态行为）**组成

###### （1）类之间的关系
包含：**关联（聚集（共享聚集，组成或组合）），继承（泛化），依赖，精化（实现）**

| 关系 | 符号 |
| :--- | :--- |
| **关联** | 实线 |
| **聚集** | 实线 + 空心菱形 |
| **组合** | 实线 + 实心菱形 |
| **泛化** | 实线 + 空心三角 |
| **依赖** | 虚线 + 箭头 |
| **实现** | 虚线 + 空心三角 |

###### （2）类图的组成与数据库映射
- **类**（类名、属性、方法/操作），逻辑设计中**转为表**
- **关系**，逻辑设计中**转为外键**

##### 2. 系统结构与顺序图
- 针对每一个特定用例，如何利用类图规范的对象来完成用例交付的任务，需要利用**顺序图/时序图**
- 主要用于描述系统内对象之间的消息发送和接收序列
- **顺序图中的所有元素需要在类图中存在**

##### 3. 系统结构与通信图
- 是交互图的一种，即**协作图**
- 用于展示对象间组织交互关系和链接，不侧重交互顺序，**用序列号来确定消息及其并发线程的顺序**
- **顺序图强调时间，通信图强调空间**

---

#### 三、DBAS 系统微观设计的表达方法
- **对象图**用于描述细节方面的内容
- **状态图、时间图**用于表达、分析、描述某个状态下系统的运行情况

##### 1. 微观设计与对象图
- 对象图是类图的实例，描述特定时间中所有对象在系统中的结构关系，**相当于一个快照**
- 当类实例化为不同的对象时，不同对象之间具有相同的属性类别和方法，但不同对象之间的属性值各不相同
- 对象图由**对象（类名.对象名称、属性及其值）、对象间的关系**组成

##### 2. 微观设计与状态机图
- 状态机图用于描述有关事件或对象的状态转移
- **只有一个起始状态，但可以有多个结束状态**
- 状态间的转移**由事件驱动**

##### 3. 微观设计与时间图
- 当状态转换**由时间驱动**时，用时间图描述状态的变化
- 描述时间驱动的状态转换，即当状态维持多少时间后转移
- 时间图中，**整个矩形框就是一个生命线**

---

#### 四、DBAS 系统宏观设计的表达方法
- 宏观设计指的是，将设计的重点放在较大范围中元素之间的联系，例如包、命名空间、子系统等
- 一个良好的命名空间满足**外部松耦合，内部高内聚**的要求

##### 1. 宏观设计与包图
- 包图表示系统中不同包、命名空间或不同项目间的关系，即逻辑层次与实体层次上的关联性

##### 2. 宏观设计与交互概述图
- **将活动图与顺序图融合**
- 以活动图为基础，在控制流之间连接交互图，将所有的交互图关系呈现出来
- 可以将不同的交互图结合在同一张图表达

##### 3. 宏观设计与复合结构图
- 用于系统之间的接口
- 是架构师在系统初期评估系统复杂度的工具，以及系统维护的参考图

---

#### 五、DBAS 系统实现与部署的表达方法

##### 1. 系统实现与组件图
- 用于表示系统的**静态实现视图**，用来展现组件之间的组织和依赖
- 用于对源代码、exe 文件的发布、物理数据库等系统建模
- **组件是逻辑设计中定义的概念和功能在物理架构中的实现**

##### 2. 系统实现与部署图
- **部署图 / 配置图**，用于描述系统中硬件和软件的物理配置情况与系统体系结构
- 部署图说明实体组件，例如 **exe 文件如何部署到计算机中**
- **在项目进行集成测试前使用**

---

### 🎯 考场核心避坑与速杀法则
1. 问：“**全都属于表达 DBAS 内部结构的系统结构图的是？**” ➔ 秒选【**类图、顺序图、通信图**】（类图描述静态，顺序图与通信图表示动态）！
2. 问：“**类图中类与关系在数据库逻辑设计中分别转化为？**” ➔ 类转化为【**表**】，关系转化为【**外键**】！
3. 问：“**最适合描述系统/子系统工作流程，类似于流程图但可描述并行操作的是？**” ➔ 秒选【**活动图**】！
4. 问：“**相当于特定时间快照，描述细节方面内容的是？**” ➔ 秒选【**对象图**】！
5. 问：“**描述时间驱动的状态转换，整个矩形框就是一个生命线的是？**” ➔ 秒选【**时间图**】！
6. 问：“**将活动图与顺序图融合，在控制流之间连接交互图的是？**” ➔ 秒选【**交互概述图**】！
7. 问：“**在项目进行集成测试前使用，说明实体组件如何部署到计算机的是？**” ➔ 秒选【**部署图（配置图）**】！`
    },
    {
      id: 'use-case-relations',
      title: '3. UML 用例图深度语法与构造型规范（include / extend / 箭头虚实 / 边界与参与者）',
      tag: '用例语法细节',
      highlightedKeywords: [
        '用例图',
        '构造型',
        '<<include>>',
        '<<extend>>',
        '扩展关系',
        '包含关系',
        '使用关系',
        '泛化关系',
        '扩展点',
        'Extension Point',
        '虚线箭头',
        '实线箭头',
        '空心三角形',
        '系统边界',
        '参与者',
        'Actor'
      ],
      content: `### UML 用例图（Use Case Diagram）深度语法与构造型规范

用例图是从外部用户（参与者）的视角展现系统功能的动态行为模型。在系统需求分析与数据库概念设计阶段，用例图用于明确系统范围、业务接口与功能边界。

---

### 一、 用例图四大基础构成图元与规范

| 组成图元 | UML 规范符号 | 语法含义与规则 | 绘制位置与注意事项 |
| :--- | :--- | :--- | :--- |
| **1. 系统边界 (System Boundary)** | **矩形框**（顶部标注系统名称） | 界定目标系统的业务实现范围。系统的内部功能放在框内，外部实体放在框外 | **所有用例必须画在矩形框内**；**所有参与者必须画在矩形框外** |
| **2. 参与者 (Actor)** | **“小人”图标** 或 标注 \`<<actor>>\` 的矩形框 | 存在于系统外部，与系统发生交互交互的人员角色、外部硬件设备、传感器或第三方外部软件系统 | 参与者不是具体的某一个人，而是一种**业务角色**；位于系统边界外部 |
| **3. 用例 (Use Case)** | **实线椭圆**（内部标注动宾短语） | 代表系统向参与者提供的一项完整、独立、可观察的业务服务或功能单元 | 椭圆内部文本必须采用**动宾短语**（如“借阅图书”、“查询余额”），画在系统边界内 |
| **4. 通信关联 (Association)** | **实线**（通常无箭头，或单向实线开放箭头） | 表达参与者与用例之间存在双向通信交互或参与者触发了该用例 | **必须用实线**！**严禁在参与者与用例之间使用虚线，严禁标注 \`<<include>>\` 或 \`<<extend>>\`** |

---

### 二、 用例之间核心关系全景对比（构造型、箭头虚实、指向与条件性）

依据 UML 统一建模规范，用例（椭圆）与用例之间主要存在以下关系：

| 关系类型 | 构造型语法 (Stereotype) | 连线线型与箭头形状 | 箭头严格指向 | 执行条件与依赖特征 | 双方认知感知 | 经典考场真题案例 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **包含/使用关系 (Include / Use)** | \`<<include>>\` (或历史规范中的 \`<<use>>\`) | **带箭头的虚线** ┈┈> (开放线形单线箭头) | **基础用例 ➔ 被包含公共用例**（正向调用指向！） | **无条件强制执行**。基础用例在执行流中**必须无条件调用**该公共用例，否则基础用例自身无法构成完整闭环 | 基础用例**明确知晓**被包含用例的存在，且强依赖其返回结果 | “借书”、“还书”用例在执行前必须包含并调用公共的“**验证读者身份**”用例；“存/取款”必须包含“**验证密码**” |
| **扩展关系 (Extend)** | \`<<extend>>\`（可标注扩展点 \`extension point\`) | **带箭头的虚线** ┈┈> (开放线形单线箭头) | **扩展用例 ➔ 基础用例**（**逆向指向！极易扣分点**） | **有条件按需触发**。只有当业务满足指定的**扩展点 (Extension Point)** 预设布尔条件时才触发执行；若条件不满足，基础用例自身仍可独立正常执行完毕 | 基础用例对扩展用例**毫无感知**，基础用例自身保持完全自洽独立性 | 正常“借书”过程中，若读者有图书超期，有条件触发“**缴纳逾期滞纳金**”；正常“买机票”触发“**购买航意险**” |
| **泛化关系 (Generalization)** | 无构造型文本 | **实线 + 空心三角形箭头** ──▷ | **子用例 ➔ 父用例**（特殊子类指向抽象父类） | **多态与特化执行**。子用例继承父用例的业务特征、目标与关联关系，并可重写或特化特定的业务规则 | 子用例知道父用例的全部规范并进行具体化 | “微信扫码支付”、“支付宝快捷支付”与“银联云闪付”都是“**在线支付**”的泛化子用例 |
| **组合关系 (Composition)** | 组合连接 | 整体与部分聚合结构 | 复合业务向细粒度子用例分解装配 | **协同拼装整体**。将多个时序不同、功能独立的细粒度子用例组合成高层复合用例 | 宏观全景视图装配 | “电子商务购物流程”由“挑选商品”、“加入购物车”、“确认订单”、“在线结算”子用例组合拼装而成 |

---

### 三、 参与者之间的关系（Actor 语法避坑）

1. **参与者之间只能存在“泛化（继承）”关系**：
   - 符号：**实线 + 空心三角形箭头**（──▷），由**特化（子）参与者指向一般（父）参与者**。
   - 语义：子参与者继承父参与者所有可以访问执行的用例，并且可以拥有自己独特的专用用例。
   - 实例：“VIP 读者”继承“普通读者”的所有基础借阅用例，并特化拥有“预约特藏古籍”用例。
2. **绝对禁忌（考场最高频陷阱）**：
   - **参与者与参与者之间绝对不能画包含（<<include>>）或扩展（<<extend>>）关系！**
   - **参与者与用例之间绝对不能画包含（<<include>>）或扩展（<<extend>>）关系！**

---

### 四、 用例图 ASCII 拓扑直观示意图

\`\`\`
┌──────────────────────────────── 系统边界 (矩形框: 图书借阅管理系统) ────────────────────────────────┐
│                                                                                                    │
│                           ┌──────────────────────┐                                                 │
│                      ┌───▶│   验证读者身份用例   │ (被包含的公共复用用例)                          │
│                      │    └──────────────────────┘                                                 │
│                      │                ▲                                                            │
│                      │                │ <<include>> (带箭头虚线: 基础指向公共)                     │
│                      │                │                                                            │
│               ┌──────────────┐        │       <<extend>> (带箭头虚线: 扩展指向基础！逆向！)        │
│               │ 借阅图书用例 │◀───────┴────────────────────────────────────┐                       │
│               └──────────────┘ (基础用例)                                  │                       │
│                      ▲                                             ┌──────────────┐                │
│                      │                                             │ 缴纳逾期罚款 │ (扩展用例,     │
│                      │ 实线通信关联 (无箭头)                       └──────────────┘  有条件触发)   │
└──────────────────────┼─────────────────────────────────────────────────────────────────────────────┘
                       │
                      웃 读者 (参与者: 位于系统边界外)
\`\`\`

---

### 五、 考场一句话秒杀题眼与防坑总结

1. 问：“**若两个用例中存在完全相同的子逻辑，应重构为什么关系？**” ➔ 秒选 **使用关系（或包含关系 include）**！
2. 问：“**若某个用例仅在满足特定扩展点条件时才被执行，属于什么关系？**” ➔ 秒选 **扩展关系 (extend)**！
3. 问：“**扩展关系 (<<extend>>) 的虚线箭头由谁指向谁？**” ➔ 秒选 **从扩展用例指向基础用例**（千万人做错的反直觉题眼，千万不能反过来！）。
4. 问：“**包含关系 (<<include>>) 的虚线箭头由谁指向谁？**” ➔ 秒选 **从基础用例指向被包含用例**！
5. 问：“**参与者与用例之间的连线类型是什么？**” ➔ 秒选 **无箭头的实线（通信关联）**，绝不是虚线或构造型！`
    },
    {
      id: 'sequence-diagram-syntax',
      title: '4. UML 顺序图（时序图）精细语法与消息标记全解（生命线 / 激活期 / 箭头虚实形状 / 同步异步 / 销毁）',
      tag: '时序语法细节',
      highlightedKeywords: [
        '顺序图',
        '时序图',
        '生命线',
        'Lifeline',
        '激活期',
        'Activation Bar',
        '执行规格',
        '同步消息',
        '异步消息',
        '返回消息',
        '自调用',
        '实心三角形箭头',
        '开放单线箭头',
        '虚线箭头',
        '销毁标记',
        '大叉号',
        '水平方向',
        '垂直方向',
        'Combined Fragments',
        'alt',
        'loop'
      ],
      content: `### UML 顺序图（Sequence Diagram / 时序图）精细语法与消息全解

顺序图（时序图）是面向对象分析与设计中最重要的动态交互图，用于展现一组对象如何通过发送和接收消息进行协同交互，**其最本质的特征是强调消息交互的时间先后顺序**。

---

### 一、 顺序图的二维空间维度意义（必考基础概念）

| 空间维度 | 对应轴线 | 核心表达意义与语法规则 | 规范要求与注意事项 |
| :--- | :--- | :--- | :--- |
| **水平方向 (X 轴 / 横轴)** | 横向排列 | 表达**参与交互的对象或角色的空间排列分布** | 从左至右通常按对象**参与交互的先后时序**排列（通常最左侧为交互的发起者，如参与者 Actor 或界面 Controller）。**对象之间的水平物理间距不代表网络距离或物理距离**，只为排版清晰 |
| **垂直方向 (Y 轴 / 纵轴)** | 纵向下延 | 表达**时间的严格流逝方向（从上到下按时间递增）** | **从图的顶部到图的底部，时间单向严格递增（Top to Bottom）**。越在下方的消息发生得越晚。两条消息纵向距离的大小只表达时间先后，通常不严格量化绝对耗时毫秒数 |

---

### 二、 顺序图核心图元构成与语法规范

#### 1. 对象角色框（Object Role Box）
- **图元位置**：位于顺序图顶部的矩形框。
- **命名规范（三种经典语法格式）**：
  1. \`对象名: 类名\`（带下划线）：代表属于指定类的具体命名实例（如 \`curOrder: Order\`）。
  2. \`:类名\`（带下划线）：**匿名对象**，只强调该类的实例角色，不关心具体实例变量名（如 \`:OrderService\`）。
  3. \`对象名\`：仅标明对象名，省略类名。

#### 2. 对象生命线（Lifeline）
- **图元样式**：从对象角色框正底方向下垂直延伸的**单条垂直虚线（Vertical Dashed Line）**。
- **语法语义**：表示该对象在系统运行交互过程中的**存活时间周期**。虚线存在即代表该对象已在内存中实例化且未被垃圾回收或释放。

#### 3. 激活期 / 执行规格（Activation Bar / Execution Specification）
- **图元样式**：覆盖在生命线上绘制的**垂直细长矩形条（白色或浅色柱状条）**。
- **语法语义**：表示对象在当前时间区间内**处于活动状态、正在执行某项操作、占用 CPU 时间片、或当前拥有系统的控制权**。
- **激活期的开始与结束**：
  - 当对象收到一条同步或异步调用消息时，**激活期开始（细长条开始出现）**；
  - 当对象操作执行完毕并返回结果时，**激活期结束（细长条闭合并恢复为单根虚线）**。
- **嵌套激活（Nested Activation）**：若对象调用自身的方法（自调用），或调用其他对象后处于等待挂起时，可以在原激活期右侧**叠放绘制一个向右偏移的第二层细长矩形条**。

#### 4. 对象销毁标记（Destruction Occurrence / Stop）
- **图元样式**：在对象生命线的最底端绘制一个**明显的大叉号（“×”号）**。
- **语法语义**：表示该对象的生命周期在该时间点正式结束，该对象已被显式析构、释放或从内存中销毁（如 C++ 的 \`delete\` 或执行连接关闭）。
- **严格制图规则**：**生命线在“×”号处立即彻底截断终止，其下方严禁继续画虚线！**

---

### 三、 消息类型、箭头虚实与箭头形状全景对照表（核心必背考点）

在顺序图中，不同的消息类型在**连线线型（实线/虚线）**、**箭头形状（实心三角/开放线形）**以及**控制权语义**上有着极其严格的规范区别：

| 消息类型 | 连线线型 | 箭头精确形状 | UML 符号图解 | 控制权与阻塞语义 | 接收端生命线变化 | 对应代码典型逻辑 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1. 同步调用消息 (Synchronous Message)** | **实线** (Solid) | **实心填充三角形箭头** | ──────▶ | **阻塞调用**。发送方发出消息后立即挂起阻塞，交出控制权，**必须等待接收方执行完成并返回**后才继续向下执行 | 接收方生命线上**立即开始新的激活期**（出现矩形柱） | 普通同步方法阻塞调用：\`int res = calc.doSum();\` |
| **2. 异步信号消息 (Asynchronous Message)** | **实线** (Solid) | **开放线形单线箭头** | ──────> | **非阻塞调用**。发送方发出消息后**无需等待接收方处理**，不交出控制权，直接并行继续向下执行后续自身逻辑 | 接收方自主启动异步任务进行处理 | 消息队列发送、事件通知、多线程启动：\`mq.send(evt);\` |
| **3. 返回消息 / 应答 (Return Message)** | **虚线** (Dashed) | **开放线形单线箭头** | ┈┈┈┈┈> | **结果交还**。被调用方执行完成，将处理结果和系统控制权交还给发起调用的原对象 | 被调用方的**当前激活期在此时刻立即闭合结束** | 方法返回值返回：\`return orderDetail;\`（若流程无歧义可省略） |
| **4. 自身消息 / 自调用 (Self Message)** | **实线折线** (向右引出再折回) | **实心填充三角形箭头** | ──┐<br>&nbsp;&nbsp;&nbsp;&nbsp;└──▶ | **内部操作**。对象自身的方法调用本对象内的另一个方法 | 生命线上产生**叠放重叠的第二层激活期**（嵌套激活） | 内部私有方法调用：\`this.validateInput();\` |
| **5. 动态创建消息 (Create Message)** | **虚线或实线** (标 \`<<create>>\` 或 \`new\`) | **开放线形单线箭头** | ──────> | **运行时实例化**。在系统执行流推进过程中动态生成一个全新的对象实例 | **箭头直接指向新建对象顶部的角色矩形框**（该矩形框垂直位置低于图顶部的其他常驻对象） | 动态新建对象：\`Order newOrd = new Order();\` |
| **6. 显式销毁通知 (Delete Message)** | **实线** | **实心或开放箭头** | ──────▶ | **通知销毁**。指示目标对象结束生命周期、清理缓存和系统资源 | 目标对象生命线在该点结束，底部标注 **“×”** 截断 | 显式销毁：\`delete targetObj;\` 或 \`session.close();\` |

---

### 四、 复合片段（Combined Fragments / 控制逻辑结构）

为了表达面向对象程序中的条件分支、循环与并发，UML 2.0 引入了**复合片段（带有操作符标签的大矩形框）**：

| 操作符标签 | 中文名称 | 程序控制结构对应 | 框内语法与划分方式 |
| :--- | :--- | :--- | :--- |
| **\`alt\`** | **互斥选择 (Alternatives)** | \`if ... else if ... else\` | 框内通过一条或多条**水平虚线**分割为多个子执行区，每个区域顶部写中括号条件保护（如 \`[balance >= amount]\`），**仅满足条件的唯一分支被执行** |
| **\`opt\`** | **单分支可选 (Option)** | \`if (condition)\` (无 else) | 仅包含一个条件区（如 \`[isVIP]\`），条件为真时执行框内消息，否则直接跳过 |
| **\`loop\`** | **循环迭代 (Loop)** | \`for\` / \`while\` 循环结构 | 标注循环次数或布尔终止条件（如 \`loop(1, 10)\` 或 \`loop [hasMoreRows]\`），重复执行框内消息流 |
| **\`par\`** | **并行并发 (Parallel)** | 多线程并发执行 | 框内用水平虚线划分为多个子并发流，各子流内的消息在运行时**同时并行交替推进** |
| **\`break\`** | **中断跳出 (Break)** | \`break\` 或异常中断 \`throw\` | 当满足特定异常条件时，跳出包围的外层复合片段，未执行的后续消息被丢弃 |

---

### 五、 顺序图完整 ASCII 拓扑架构图解

\`\`\`
       :User (用户参与者)            :OrderController                :InventoryService
              │                              │                              │
     X 轴:    │                              │                              │
   对象空间排列│                              │                              │
              │                              │                              │
     Y 轴:    │ 1: createOrder()             │                              │
    从上向下  │─────────────────────────────▶│                              │
    时间流逝  │ (实线+实心三角: 同步调用)     │                              │
              │                              │                              │
              │                              │ 2: checkStock()              │
              │                              │─────────────────────────────▶│
              │                              │ (实线+实心三角: 阻塞调用)    │ ── 激活期开始
              │                              │                              │ █ (垂直细长条柱)
              │                              │ 3: stockOK                   │ █
              │                              │◀┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈│ █ (激活期在此结束)
              │                              │ (虚线+开放箭头: 返回消息)    │
              │                              │                              │
              │                              │ 4: logTrace()                │
              │                              │──┐ (自身消息: 折返实线)      │
              │                              │  │ 产生嵌套激活期            │
              │                              │◀─┘                           │
              │                              │                              │
              │                              │ 5: asyncSendEmail()          │
              │                              │─────────────────────────────>│ ── 异步消息
              │                              │ (实线+开放单线箭头: 不等待)  │    (实线开放箭头)
              │                              │                              │
              │ 6: returnSuccess             │                              │
              │◀┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈│                              │
              │ (虚线+开放箭头: 同步调用返回)│                              │
              │                              │ 7: destroy()                 │
              │                              │─────────────────────────────▶│
              │                              │                              │
              │                              │                              × ── 销毁标记(大叉号)
              │                              │                            (生命线在此彻底截断)
\`\`\`

---

### 六、 顺序图考场速记口诀与必杀题眼

1. 问：“**顺序图（时序图）的横轴（水平方向）和纵轴（垂直方向）分别表示什么？**”
   - ➔ 横轴表示 **对象的空间并列分布**；纵轴表示 **时间的严格流逝（从上到下按时间递增）**！
2. 问：“**同步消息与异步消息在连线与箭头上有何严格区别？**”
   - ➔ **同步消息** 是 **实线 + 实心填充三角形箭头 (──▶)**；
   - ➔ **异步消息** 是 **实线 + 开放单线箭头 (──>)**！
3. 问：“**返回消息（Reply/Return）的连线与箭头是什么？**”
   - ➔ **虚线 + 开放单线箭头 (┈┈>)**！
4. 问：“**生命线上的细长矩形条代表什么？生命线末尾的大叉号（×）代表什么？**”
   - ➔ 矩形条代表 **激活期（或执行规格）**，表示对象处于活动运行状态；
   - ➔ 大叉号代表 **对象被销毁（生命周期终结）**，生命线在此处截断，下方不再延伸！
5. 问：“**动态创建新对象的创建消息箭头直接指向何处？**”
   - ➔ 直接指向 **新建对象的顶部角色矩形框**（该矩形框位置低于顶层其他常驻对象）！`
    },
    {
      id: 'activity-diagram-details',
      title: '5. UML 活动图泳道与并发控制流细节（Swimlane / Fork / Join）',
      tag: '控制流细节',
      highlightedKeywords: [
        '活动图',
        '泳道',
        'Swimlane',
        '分叉',
        'Fork',
        '汇合',
        'Join',
        '分支',
        '合并',
        '初态',
        '终态'
      ],
      content: `### UML 活动图（Activity Diagram）泳道与并发控制流细节

活动图是阐明业务工作流、操作流程以及算法逻辑执行顺序的动态行为图。

#### 1. 活动图核心特色元素
- **泳道（Swimlane）**：
  - 用垂直或水平的矩形通道将活动图划分为若干平行区域；
  - 核心作用：**明确标明每个业务活动的执行责任人、经办部门或所属软件构件**。
- **分支与合并（菱形图元）**：
  - **分支 (Decision)**：一个输入流根据布尔监护条件（Guard Condition \`[条件]\`）分出多个互斥的输出流。
  - **合并 (Merge)**：多个互斥流汇入同一个出口继续向下流转。
- **分叉与汇合（粗黑实线水平棒 / 垂直棒，Fork / Join）**：
  - **分叉 (Fork)**：一个输入流同时分出多个**并发执行的并行子流**。
  - **汇合 (Join)**：所有并发前驱子流全部执行完毕后，才能继续向下执行（**同步栅栏屏障机制**）。
- **开始节点与结束节点**：
  - **初态（Initial Node）**：单个实心黑圆点（系统流的起点，一张图通常只有 1 个）。
  - **活动终态（Activity Final Node）**：双圆圈内含实心圆（流程整体终止，可以有多个）。
  - **流终态（Flow Final Node）**：圆圈内带一个大叉号（仅终止当前单条流，不终止整个活动）。`
    },
    {
      id: 'class-relationships',
      title: '6. 类图六大关系符号速查表',
      tag: '符号规范',
      highlightedKeywords: [
        '泛化/概括',
        '实现',
        '组合',
        '聚合',
        '关联',
        '依赖',
        '实心菱形',
        '空心菱形',
        '聚集关系',
        'is-a',
        'has-a',
        'part-of',
        'uses-a'
      ],
      content: `### 类图六大关系强度递增排序与绘图符号

在类图中，六种关系的耦合强度从弱到强排列为：
**依赖 < 关联 < 聚合 < 组合 < 实现 < 泛化**

| 关系名称 | 现实语义比喻 | UML 符号与连线样式 | 代码中典型表现 |
| :--- | :--- | :--- | :--- |
| **依赖 (Dependency)** | “临时使用”关系（如人使用手机发短信） | **带箭头的虚线** ⇢ | 局部变量、方法参数、静态方法调用 |
| **关联 (Association)** | “长久拥有/知道”关系（如老师与学生） | **带箭头的实线**（单向）或 无箭头实线（双向） | 类的成员变量（Field 引用） |
| **聚合 (Aggregation)** | “整体与部分，但可分离”关系（如学校与学生） | **空心菱形 + 实线**（菱形端指向整体） | 成员变量，整体销毁后部分**依然存活** |
| **组合 (Composition)** | “同生共死，强隶属”关系（如大雁与翅膀，公司与部门） | **实心菱形 + 实线**（实心菱形端指向整体） | 成员变量，整体销毁后部分**同步被销毁** |
| **实现 (Realization)** | 类实现接口中的抽象规范 | **空心三角形箭头 + 虚线**（箭头指向接口） | \`class Car implements Vehicle\` |
| **泛化/概括 (Generalization)**| 面向对象的父子类继承关系 | **空心三角形箭头 + 实线**（箭头指向父类） | \`class Dog extends Animal\` |

---

### UML 4 大顶层关系与开发中 6 种细分映射

UML 官方标准（OMG 规范）最初定义的四大顶层关系就是：**依赖、关联、泛化、实现**。

\`\`\`text
【UML 4 大顶层关系】                      【面向对象/开发中的 6 种细分】
1. 依赖 (Dependency)  ───────────────>  ① 依赖 (uses-a)
2. 关联 (Association) ──┬────────────>  ② 关联 (has-a)
                        │（弱包含）
                        ├────────────>  ③ 聚合 (has-a，空心菱形，整体和部分可分离)
                        │（强包含）
                        └────────────>  ④ 组合 (contains-a，实心菱形，同生共死)
3. 泛化 (Generalization) ────────────>  ⑤ 泛化/概括 (is-a，类继承类)
4. 实现 (Realization)   ─────────────>  ⑥ 实现 (like-a，类实现接口)
\`\`\`

1. **聚合与组合都属于“关联关系”的特例**：
   - 在早期的数据库教材和部分考题中，命题老师常把“聚合（Aggregation）”和“组合（Composition）”统称为**聚集关系**或直接归在**关联**底下。

2. **实现是“泛化”的一种特殊形态**：
   - **泛化**是“子类继承父类”（实线空心三角）；
   - **实现**是“类实现接口”（虚线空心三角），本质上也是一种契约继承。

---

### UML 核心关系现实语义与造句标志词速查

| UML 关系名称 | 现实语义 | 经典造句 / 标志词 |
| :--- | :--- | :--- |
| **概括关系（泛化）** | **is-a**（是一种，继承） | “轿车是汽车”、“售票员是业务员” |
| **关联关系** | **has-a / knows-a**（拥有、协同） | 平级且长期稳定联系，如“老师教学生”、“顾客下订单” |
| **聚集关系（聚合/组合）** | **part-of**（整体与部分） | “汽车由发动机、轮胎组成”、“学院由系组成” |
| **依赖关系** | **uses-a**（偶然使用） | 弱依赖，如“人骑自行车上班”（参数传递、临时调用） |`
    }
  ]
};
