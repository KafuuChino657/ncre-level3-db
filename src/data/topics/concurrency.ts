import { Topic } from '../../types';

export const concurrencyTopic: Topic = {
  id: 'concurrency-deadlock',
  title: '事务并发、锁机制与死锁调优',
  badge: '底层机制',
  icon: 'ShieldCheck',
  description: 'ACID四大特性、并发异常、三级封锁协议、两段锁协议充分条件、封锁粒度层级树、意向锁(IS/IX/SIX)、锁升级、死锁优化万能公式',
  subTopics: [
    {
      id: 'acid-and-anomalies',
      title: '1. 事务 ACID 特性与并发四大异常',
      tag: '核心理论',
      highlightedKeywords: [
        '脏读',
        '不可重复读',
        '幻读',
        '丢失修改',
        '原子性',
        '一致性',
        '隔离性',
        '持久性'
      ],
      content: `### 事务 ACID 特性及其保证子系统

| 特性英文 | 中文名称 | 含义 | DBMS 底层保障机制 |
| :--- | :--- | :--- | :--- |
| **Atomicity** | **原子性** | 事务中所有操作严格遵循“全做或全不做”（不可分割的整体工作单位） | **恢复管理子系统（Undo 日志回滚）** |
| **Consistency** | **一致性** | 事务执行前后，数据库必须处于合法的语义状态 | 完整性约束检查 + 应用程序业务逻辑 |
| **Isolation** | **隔离性** | 各并发事务之间互不干扰，如同单事务独立运行 | **并发控制子系统（封锁机制与 MVCC）** |
| **Durability** | **持久性** | 事务一旦 Commit，对数据的改动永久保存在磁盘 | **恢复管理子系统（Redo 日志重做）** |

#### 并发操作带来的 4 种数据不一致性：
1. **丢失修改（Lost Update）**：事务 T1 与 T2 同时读取同一数据并修改，T2 的提交结果覆盖了 T1 的修改，导致 T1 的修改丢失。
2. **脏读（Dirty Read）**：T1 修改了数据但**尚未提交**，T2 读取了该修改后的数据；随后 T1 发生回滚，T2 读到的数据成为虚假无效数据。
3. **不可重复读（Non-repeatable Read）**：T1 读取数据后，T2 对该数据进行了 **UPDATE 或 DELETE** 并提交；T1 再次读取时发现数据值变了或记录消失了。
4. **幻读 / 虚读（Phantom Read）**：T1 按照某个条件范围读取一批数据后，T2 **INSERT（插入）** 了符合条件的新行并提交；T1 再次按相同条件查询时，发现莫名多了几行。`
    },
    {
      id: 'locking-protocols',
      title: '2. 封锁机制与三级封锁协议对比',
      tag: '协议对比',
      highlightedKeywords: [
        'X锁',
        'S锁',
        '一级封锁协议',
        '二级封锁协议',
        '三级封锁协议',
        '两段锁协议'
      ],
      content: `### 基本锁类型与封锁协议

#### 1. 排他锁（X锁）与共享锁（S锁）相容矩阵
- **排他锁（Exclusive Lock, X锁 / 写锁）**：若事务 T 对数据项加了 X 锁，只允许 T 读取和修改该数据，**其他任何事务既不能加 S 锁，也不能加 X 锁**。
- **共享锁（Shared Lock, S锁 / 读锁）**：若事务 T 对数据项加了 S 锁，其他事务**只能对其再加 S 锁，而不能加 X 锁**。

| 当前持有 \\ 申请加锁 | S 锁（读） | X 锁（写） |
| :---: | :---: | :---: |
| **S 锁** | **兼容 (OK)** | 不兼容 (等待) |
| **X 锁** | 不兼容 (等待) | 不兼容 (等待) |

#### 2. 三级封锁协议对比表（解决问题层层递进）
| 协议级别 | 加锁与解锁规则 | 防范的并发异常 | 遗留问题 |
| :--- | :--- | :--- | :--- |
| **一级封锁协议** | 修改数据前必须加 **X 锁**，直至**事务结束才释放**；单纯读不加锁 | **防止丢失修改** | 无法防止脏读和不可重复读 |
| **二级封锁协议** | 在一级基础上，读取数据前必须加 **S 锁**，**读完立即释放 S 锁** | **防止丢失修改、防止脏读** | 无法防止不可重复读 |
| **三级封锁协议** | 在一级基础上，读取数据前加 **S 锁**，且**必须等事务结束才释放 S 锁** | **防止丢失修改、脏读、不可重复读** | 开销最大，并发度最低 |

#### 3. 两段锁协议（2PL, Two-Phase Locking）
- **规则**：事务分为两个独立阶段：
  1. **扩展阶段（加锁阶段）**：只能申请获得新锁，不能释放任何锁；
  2. **收缩阶段（解锁阶段）**：只能释放锁，不能再申请任何新锁。
- **核心结论（考场必背）**：
  - 若所有并发事务均遵守两段锁协议，则调度是**可串行化的（正确性得到保证）**；
  - **两段锁是可串行化的充分条件，而非必要条件**；
  - **两段锁协议不能避免死锁**！遵守 2PL 的事务仍有可能发生死锁。`
    },
    {
      id: 'lock-granularity-and-intent',
      title: '3. 封锁粒度层级树、意向锁 (IS/IX/SIX) 与锁升级 (Lock Escalation)',
      tag: '高频底层',
      highlightedKeywords: [
        '封锁粒度',
        '行级锁',
        '页级锁',
        '区级锁',
        '表级锁',
        '数据库级锁',
        '意向锁',
        'IS锁',
        'IX锁',
        'SIX锁',
        '锁升级',
        'Lock Escalation'
      ],
      content: `### 封锁粒度（Lock Granularity）与多粒度封锁机制

在数据库管理系统（DBMS，尤其是 SQL Server / MySQL 等关系型数据库）中，**封锁粒度（Lock Granularity）** 指的是被封锁的**数据对象的大小或范围**。

封锁粒度遵循核心权衡规律：
- **粒度越细（如行锁）**：并发度极高，但系统维护锁消耗的内存和 CPU 开销很大。
- **粒度越粗（如表锁、库锁）**：系统开销极小，容易管理，但并发度极低，容易造成严重的排队阻塞。

---

### 一、 数据库封锁粒度层级树

从大到小（自顶向下），数据库中的锁粒度通常呈树状层级分布：

\`\`\`text
       数据库（Database）       <-- 粗粒度（并发低，开销小）
            │
        数据表（Table）
            │
      区 / 范围（Extent）
            │
        数据页（Page）
            │
        数据行（Row / RID / Key） <-- 细粒度（并发高，开销大）
\`\`\`

---

### 二、 常见粒度锁详细解析

| 粒度级别 | 锁定对象 | 并发度与系统开销 | 典型应用场景与特点 |
| :--- | :--- | :--- | :--- |
| **1. 行级锁<br>(Row-level / RID / Key)** | 数据表中的具体某一行记录（堆表中为 **RID 锁**，有聚集索引表中为 **Key 键锁**） | **并发度最高**<br>开销最大（消耗大量锁内存） | 事务 A 修改行 1 时，事务 B 可自由修改行 2，互不干扰；若批量更新 10 万行，会创建 10 万个锁对象 |
| **2. 页级锁<br>(Page-level Lock)** | 一个物理存储数据页（在 SQL Server 中通常为 **8KB**），通常包含数十到数百行 | 并发度与开销介于行级锁与表级锁之间 | 当查询或更新集中在同一页的多行时，使用页级锁可显著减少锁对象的数量 |
| **3. 区级锁<br>(Extent-level Lock)** | 一个连续物理存储区（SQL Server 中 1 个区 = 8 个连续数据页 = **64KB**） | 偏粗粒度，开销较小 | 通常发生在**空间分配或管理操作**中（例如表快速扩容，需要分配全新的物理页区） |
| **4. 表级锁<br>(Table-level Lock)** | 整张物理数据表 | **并发度极低**<br>开销极小（仅需维护一个锁标记） | 只要一个事务给表加了排他锁，其他事务任何读写均被阻塞。常见于：**缺少索引的全表 UPDATE/DELETE**、执行 DDL 改表结构（\`ALTER TABLE\`）、显式表锁提示（\`WITH (TABLOCK)\`）或发生**锁升级** |
| **5. 数据库级锁<br>(Database-level Lock)** | 整个数据库实例 | 并发度最低（单用户排他）<br>开销最小 | 数据库启动初始化、备份还原、或者将数据库设置为单用户模式（\`RESTRICTED_USER\` / \`SINGLE_USER\`） |

---

### 三、 跨粒度协调利器：意向锁（Intent Lock）

当系统同时支持行锁和表锁等多种粒度时，会产生致命的性能难题：
- 假设事务 A 锁住了表中的**某一行**（加排他 X 锁）；
- 此时事务 B 想要给**整个表**加一把排他表锁（X 锁）；
- **矛盾点**：如果没有额外机制，事务 B 必须从头到尾**扫描遍历表里的千万条记录**，逐一检查是否有人正持有行锁，系统性能将彻底崩溃！

为了解决这个问题，DBMS 引入了**意向锁（Intent Lock）**，主要包含三种核心类型：

1. **意向共享锁（IS, Intent Shared）**：表示“事务打算在该表下层的某一行或某一页加**共享锁（S 读锁）**”。
2. **意向排他锁（IX, Intent Exclusive）**：表示“事务打算在该表下层的某一行或某一页加**排他锁（X 写锁）**”。
3. **共享意向排他锁（SIX, Shared Intent Exclusive）**：表示“整张表我都加共享锁读了，并且打算修改其中某些具体的行（底层加 X 锁）”。

#### 意向锁运作机制：
- 事务 A 在锁住第 5 行之前，会**先在整个表上顺手打一个 \`IX\`（意向排他）标记**。
- 后续事务 B 想要申请整个表的表级 X 锁时，**只要看表头上的意向锁标记即可**！发现表头有 \`IX\` 存在，立刻知道底层必定有人正在改数据，直接原地等待，**完全无需遍历全表行记录**。

#### 意向锁与表级锁相容矩阵（考场高频）：
| 表上已有锁 \\ 申请新锁 | IS | IX | S | SIX | X |
| :---: | :---: | :---: | :---: | :---: | :---: |
| **IS** | **兼容** | **兼容** | **兼容** | **兼容** | 不兼容 (等待) |
| **IX** | **兼容** | **兼容** | 不兼容 | 不兼容 | 不兼容 |
| **S** | **兼容** | 不兼容 | **兼容** | 不兼容 | 不兼容 |
| **SIX** | **兼容** | 不兼容 | 不兼容 | 不兼容 | 不兼容 |
| **X** | 不兼容 | 不兼容 | 不兼容 | 不兼容 | 不兼容 |

> 💡 **秒杀口诀**：
> 1. **意向锁之间基本相互兼容**（IS 与 IS、IS 与 IX、IX 与 IX 均兼容，因为它们只代表“要在底层找某一行加锁”，彼此不干涉具体哪一行）；
> 2. 只要有事务想申请**表级排他锁 (X)**，与任何已有的表锁或意向锁**全都不兼容**！

---

### 四、 什么是锁升级（Lock Escalation）？

- **定义**：当一个事务持有的细粒度锁（如行锁）数量超过预设阈值（SQL Server 默认约为 **5000 个**）且锁管理器内存压力较大时，DBMS 会自动把成千上万个**细粒度行级锁合并为一个粗粒度表级锁**。
- **利（优点）**：瞬间释放数十兆甚至上百兆的锁管理内存，极大减轻 CPU 锁管理器的遍历开销。
- **弊（缺点）**：
  1. **瞬间扼杀并发能力**：原本只影响特定行的操作，升级后把整张表完全锁死；
  2. **极易诱发死锁（Deadlock）**：如果两个事务各自持有一部分行锁，同时因批量更新触发锁升级、试图向同一个表申请表级排他锁时，双方互相等待对方释放行锁，直接诱发死锁。`
    },
    {
      id: 'deadlock-detection-and-tuning',
      title: '4. 死锁检测图 (Wait-for Graph) 与降低死锁五大准则',
      tag: '死锁对策',
      highlightedKeywords: [
        '死锁',
        '等待图',
        'Wait-for Graph',
        '有向环路',
        '死锁牺牲者',
        '降低死锁',
        '按固定顺序'
      ],
      content: `### 死锁检测与降低死锁五大准则

#### 1. 死锁的检测与解除
- **等待图（Wait-for Graph）**：
  - 图中节点代表事务（如 $T_1, T_2$）；
  - 有向边 $T_1 \\to T_2$ 代表事务 $T_1$ 正在等待 $T_2$ 释放所占有的资源。
  - **核心判定结论**：**如果在等待图中检测到有向环路（Cycle），则判定系统发生了死锁**！
- **解除方法（选择牺牲者 Victim）**：
  - DBMS 死锁检测线程定期扫描等待图；
  - 发现环路后，选择环路中**代价最小（已执行时间最短、修改记录数最少、回滚代价最低）**的事务作为牺牲者，自动执行回滚（Rollback），释放其占有的全部锁资源，让其他事务得以顺利推进。

---

#### 2. 降低死锁发生概率的五大设计准则（大题万能答题点）

1. **按固定的先后顺序访问资源（最关键准则）**：
   - 规定所有业务模块对数据表、记录的访问必须严格遵循相同的全局顺序（如按主键升序或固定表顺序），从根本上破坏“循环等待”条件。
2. **缩短事务规模，将其拆分为小事务**：
   - 事务执行时间越长，持锁跨度越久，与其他事务发生资源冲突的概率呈指数级上升。
3. **在同一个事务中，尽量一次性锁定所有需要的资源**：
   - 避免在事务执行过程中零散、动态地频繁申请新锁。
4. **合理设置事务隔离级别**：
   - 避免盲目使用最高级别的 Serializable（串行化）；在业务允许的前提下使用 Read Committed（读已提交）或快照隔离（Snapshot Isolation / MVCC）。
5. **为数据表建立合理的索引**：
   - 缺少索引会导致查询执行全表扫描，促使数据库引擎将细粒度行锁升级（Lock Escalation）为整表锁，极易与其他并发事务形成资源互斥死锁。`
    }
  ]
};
