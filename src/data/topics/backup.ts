import { Topic } from '../../types';

export const backupTopic: Topic = {
  id: 'backup-recovery',
  title: '备份恢复与系统数据库维护',
  badge: '安全容灾',
  icon: 'ShieldCheck',
  description: '三种转储对比、WAL日志先写原则、检查点机制、软故障UNDO/REDO恢复与四大故障类型、四大系统库维护、分离与附加数据库、数据库镜像三大模式',
  subTopics: [
    {
      id: 'backup-types-comparison',
      title: '1. 数据库备份类型、常用策略组合与恢复执行机制（含语法与题眼）',
      tag: '高频核心策略',
      highlightedKeywords: [
        '完整数据库备份',
        '差异数据库备份',
        '文件备份',
        '差异文件备份',
        '事务日志备份',
        '完全转储',
        '差量转储',
        '增量转储',
        '恢复顺序',
        'WITH NORECOVERY',
        'WITH INIT',
        'WITH RECOVERY',
        '日志链',
        '时间点还原',
        '黄金组合',
        '恢复模式',
        '完整模式',
        '简单模式',
        '大容量日志',
        '纯日志备份',
        '大容量操作日志备份',
        '结尾日志备份',
        '尾部日志',
        'BACKUP DATABASE',
        'WITH DIFFERENTIAL',
        'BACKUP LOG',
        'RESTORE LOG'
      ],
      content: `### 数据库备份类型、恢复策略与恢复执行机制

---

#### 一、 备份类型与支持级别（官方标准分类）
SQL Server 恢复机制支持两大级别：**数据库（DB）级恢复** 与 **文件级恢复**。

| 备份大类 | 具体备份类型 | 核心定义与备份内容 | 恢复特点与考点题眼 |
| :--- | :--- | :--- | :--- |
| **数据库备份** | **完整数据库备份**<br>*(完全转储)* | 恢复 DB 的基线。需要备份：<br>1. **整个 DB**（数据文件、DB 对象、数据信息）；<br>2. 备份处于**备份过程时发生的活动**；<br>3. 备份**未提交的事务到日志**。 | 文件最大，但**唯一能独立完成还原**，是所有其他备份的基准（地基）。 |
| | **差异数据库备份**<br>*(差异转储)* | 需要以上次完整备份为基准点。需要备份：<br>1. 从**上次完备之后数据的改动**；<br>2. 处于**差异备份过程时发生的活动**；<br>3. 备份**未提交的事务到日志**。 | 比完备节约时间；**无法独立还原**，恢复时必须先恢复对应的完整备份。 |
| **文件备份** | **文件备份** | 针对数据库物理文件进行备份和还原。 | **可以只恢复已损坏的 DB 文件**，更灵活，恢复速度更快。 |
| | **差异文件备份** | 备份从**上次文件备份之后数据的改动**。 | 仅针对物理文件变更，减少恢复时间。 |
| **事务日志备份** | **事务日志备份** | 备份自上次日志备份之后的日志记录。分为纯日志、大容量操作日志和结尾日志（尾日志）。 | 仅用于**完整恢复模式**或**大容量日志恢复模式**；**唯一支持时间点还原**。 |

---

#### 二、 常用备份策略与适用场景

| 备份策略 | 适用场景及特征 | 考场标志题眼 |
| :--- | :--- | :--- |
| **完整备份** | • 适用于 DB **数据量较小、数据更改不频繁**的情况<br>• 一般**几天或几周一次**<br>• 若对数据修改不频繁且**允许少量数据丢失**，可以只用完备策略 | “数据量小、更改不频繁、允许少量丢失” ➔ **完整备份** |
| **完整备份 + 日志备份** | • 适用于**只允许极少数据丢失**，同时**备份频率较低**的情况<br>• 在两次完全备份期间进行日志备份 | “极少数据丢失、低频完备+高频日志” ➔ **完备 + 日志** |
| **完整备份 + 差异备份 + 日志备份** | • 在两次完备之间插入若干差异备份，在若干差异备份之间插入若干日志备份<br>• 黄金搭档：兼顾极高备份效率与极快灾难恢复速度 | “大幅缩短日志重放时间、兼顾速度与安全” ➔ **完备 + 差备 + 日志** |

---

#### 三、 数据库恢复的严格顺序（标准四步法）
当发生系统瘫痪或介质损毁需要还原时，系统必须按照以下**严格先后次序**逐步应用备份集：

1. **第一步：恢复最近的完全备份**（筑牢底层数据与结构基线）；
2. **第二步：恢复最近的差异备份（若有）**（跳过前期大量繁杂的日志文件）；
3. **第三步：恢复差异备份之后的日志备份（若有）**（按日志链顺序重放到故障前最后一刻）；
4. **第四步：恢复数据库**（应用事务日志修平并使数据库联机上线）。

---

#### 四、 备份与恢复的 T-SQL 语法实现与核心选项

##### 1. 备份实现（BACKUP）
- **完全备份**：
  \`\`\`sql
  BACKUP DATABASE 数据库名 TO 备份设备
  \`\`\`
- **覆盖完全备份（WITH INIT）**：
  \`\`\`sql
  BACKUP DATABASE 数据库名 TO 备份设备 WITH INIT
  \`\`\`
  > 💡 **\`WITH INIT\`**：表示**覆盖重写**备份介质上的现有内容（重置为新备份集）；默认不带 INIT 则为追加。
- **差异备份（WITH DIFFERENTIAL）**：
  \`\`\`sql
  BACKUP DATABASE 数据库名 TO 备份设备 WITH DIFFERENTIAL
  \`\`\`
- **追加日志备份**：
  \`\`\`sql
  BACKUP LOG 数据库名 TO 备份设备
  \`\`\`
  > ⚠️ 开头必须是 **\`BACKUP LOG\`**，绝不能带 DATABASE！

##### 2. 恢复实现与 WITH NORECOVERY（核心真题高频项）
- **完全备份恢复**：
  \`\`\`sql
  RESTORE DATABASE 数据库名 FROM 完全备份设备 WITH NORECOVERY
  \`\`\`
- **差异备份恢复**：
  \`\`\`sql
  RESTORE DATABASE 数据库名 FROM 差异备份设备 WITH NORECOVERY
  \`\`\`
- **日志备份恢复**：
  \`\`\`sql
  RESTORE LOG 数据库名 FROM 日志备份设备 WITH NORECOVERY
  \`\`\`
- **恢复数据库（最后一步）**：
  \`\`\`sql
  RESTORE DATABASE 数据库名 WITH RECOVERY
  \`\`\`
> 🎯 **\`WITH NORECOVERY\` 题眼速记**：
> - **核心含义**：**明确告知系统“之后还有备份文件需要继续恢复”**！
> - **底层状态**：使数据库始终处于**正在恢复（RESTORING）**状态，暂不回滚未提交的事务，以便后续继续追加日志或差异备份。如果最后一笔不写或写 **\`WITH RECOVERY\`**，则数据库正式完成恢复并恢复上线。

---

#### 五、 日志备份的三大具体类型对比
| 日志备份类型 | 核心特征与定义 | 适用场景 |
| :--- | :--- | :--- |
| **纯日志备份** | 仅包含特定时间间隔内的**常规事务日志记录**，不包含大容量操作修改的数据页。 | 完整恢复模式下的日常定期备份。 |
| **大容量操作日志备份** | 包含日志记录以及由**大容量操作（Bulk-logged）修改的数据页**。 | 大容量日志恢复模式下批量导入后备份（不允许时点恢复）。 |
| **结尾日志备份（尾日志）** | **专用于可能已损坏、离线或准备还原的数据库**，捕获当前活动中尚未备份的最新尾部记录。 | **灾难发生后、启动还原前必须先执行**，达成零数据丢失！ |

---

#### 六、 SQL Server 数据库的三种恢复模式对比
| 恢复模式 | 核心特征 | 日志空间与维护 | 考场标志场景 |
| :--- | :--- | :--- | :--- |
| **简单恢复模式** | 不支持日志备份，只能还原到完整/差异备份点 | 提交后自动截断日志，维护成本极低 | 只读库、数据仓库、测试/开发库 |
| **完整恢复模式** | 记录所有事务细节，支持还原到任意时间点（Point-in-Time） | 日志不自动释放，必须定期备份日志，否则占满磁盘 | 核心 OLTP 生产交易系统（银行、订单库） |
| **大容量日志恢复模式** | 大批量操作最小化日志记录，提升导入性能 | 无法还原到任意时间点 | 偶尔海量数据批处理导入时的临时切换状态 |

---

#### 七、 考场高频正误判断陷阱
- **陷阱 1**：“更改频繁、严禁丢数据的系统适合只做完整备份” ➔ **错误**（必须配合日志备份）。
- **陷阱 2**：“仅凭差异备份就能将数据库恢复出来” ➔ **错误**（差异备份无独立还原能力，基线必须是对应的完整备份）。
- **陷阱 3**：“完整备份 + 日志备份的还原速度比带差异备份的更快” ➔ **错误**（带差异备份还原速度快得多）。
- **陷阱 4**：“只有日志备份支持恢复到事务发生前的指定时点” ➔ **正确**。`
    },
    {
      id: 'wal-and-checkpoint',
      title: '2. 先写日志原则（WAL）、检查点与日志文件恢复场景矩阵（含故障类型全景）',
      tag: '日志与故障恢复',
      highlightedKeywords: [
        '先写日志原则',
        'WAL',
        '检查点',
        'Checkpoint',
        'REDO',
        'UNDO',
        '软故障',
        '硬故障',
        '介质故障',
        '事务内部故障',
        '预期的事务内部故障',
        '非预期的事务内部故障',
        '系统故障',
        '动态转储',
        '静态转储',
        '日志文件',
        '以记录为单位的日志文件',
        'Record-level',
        '以数据块为单位的日志文件',
        'Block-level',
        '物理块镜像',
        'Before Image / After Image'
      ],
      content: `### 日志文件、检查点与故障恢复核心考点精讲

---

### 零、 官方教材的两种日志文件格式（高频题眼）

日志文件用来记录事务对数据库的所有更新操作。按物理组织颗粒度，官方教材明确划分为两种：

#### 1. 以记录为单位的日志文件（Record-level）
- **记录内容**：
  - 每个事务的开始标记（\`BEGIN\`）；
  - 事务结束标记（\`COMMIT\` / \`ROLLBACK\`）；
  - 以及具体的每一次更新操作（包含：**事务标识**、**操作类型**、**操作对象**、**更新前旧值/前像**、**更新后新值/后像**）。
- **特点**：**粒度细**，记录的是一条一条逻辑记录的变更。

#### 2. 以数据块为单位的日志文件（Block-level · 本题考点）
- **记录内容**：
  - 事务对数据库的修改直接记录在物理块级别。
  - 日志中存放的是**事务标识**以及被更新的数据块在更新前后的**整个物理块镜像（Before Image / After Image）**。
- **特点**：**粒度粗**，直接保存整个数据块（Block/Page），恢复时直接用数据块覆盖，但日志膨胀较快。

---

### 一、 先写日志原则（Write-Ahead Logging, WAL）
- **核心铁律**：**在把内存中的脏数据页写入磁盘之前，必须先将对应的日志记录（Log Record）安全刷新到磁盘日志文件中！**
- **为什么必须先写日志？**
  - 如果允许先写数据页到磁盘，一旦在写日志记录前系统发生断电或崩溃，磁盘上已经残留了尚未提交事务的“脏数据”，而此时磁盘上根本没有对应的历史日志，**DBMS 将无法执行 UNDO 撤销回滚**，从而彻底破坏数据库的原子性（Atomicity）与一致性（Consistency）。
  - 因此，先写日志是保证事务原子性与持久性的最底层技术基石。

---

### 二、 检查点（Checkpoint）技术的核心作用
传统日志恢复若从系统最初的日志开始从头扫描，在庞大的工业级数据库中恢复开销将极其巨大。为此 DBMS 引入了**检查点（Checkpoint）机制**：
- **检查点周期的固定动作**：
  1. 将当前内存日志缓冲区中的所有日志记录强制写盘；
  2. 将所有内存脏数据缓冲块刷新写入磁盘数据文件；
  3. 在日志文件中写入一条 \`CHECKPOINT\` 记录，并记录该时刻系统中**所有正在活跃执行的活动事务列表**。
- **考场必背题眼**：
  - **检查点技术的核心目的**：**大幅缩短数据库发生故障后系统恢复所需的时间**（发生故障重启后，系统只需从最近一个检查点开始往前逆向扫描日志，无需遍历从头到尾的整个历史日志文件）。

---

### 三、 数据库发生软故障（系统故障）的核心恢复机理：UNDO 与 REDO

> **【机考核心金句】**：
> **“数据库发生软故障（系统故障）重启后，DBMS 需要且会自动进行 UNDO（撤销）和 REDO（重做）操作！”**

#### 为什么软故障恢复既需要 UNDO，又需要 REDO？
当系统突然断电或操作系统崩溃（软故障）时，内存数据全部丢失，但磁盘数据和日志依然完好。根据事务提交时刻，内存与磁盘的状态出现两类情况：

1. **对于故障发生前【已经提交（COMMIT）】的事务**：
   - **状态痛点**：该事务虽然已经成功提交，但由于操作系统的延迟写盘策略，其修改后的最新数据可能**仅仅保存在内存缓冲区，尚未真正刷入磁盘数据文件**！断电导致内存中的修改丢失。
   - **恢复动作**：**必须执行 REDO（重做）操作**。从日志中重读该事务的数据更新记录（After Image / 之后值），重新写入磁盘，保证事务的**持久性 (Durability)**。

2. **对于故障发生时【尚未提交（未 COMMIT / 活跃中）】的事务**：
   - **状态痛点**：故障发生时该事务仍在执行过程中，其部分中间步骤的“半成品数据”可能**已经被 DBMS 刷入了磁盘数据文件**。断电导致该事务非正常中断。
   - **恢复动作**：**必须执行 UNDO（撤销 / 回滚）操作**。从日志中逆向读取该事务的更新前旧值（Before Image / 之前值），将磁盘中被污染的数据恢复为事务开始前的原貌，消除未提交事务对数据库的影响，保证事务的**原子性 (Atomicity) 与一致性 (Consistency)**。

> 💡 **考场秒杀提示**：
> - 软故障的恢复由 **DBMS 在系统重新启动后完全自动执行**，**不需要管理员人工干预**！

---

### 四、 数据库四大故障类型对比表（高频客观题必背）

在历年三级考试中，区分故障类型、典型起因、是否需要人工干预以及恢复手段是高频考点：

| 故障类型 | 典型原因 | 是否需要管理员人工干预？ | 核心恢复手段 | 考点深度剖析与机制 |
| :--- | :--- | :--- | :--- | :--- |
| 事务内部故障（预期 / 非预期） | 运算溢出、死锁被选为牺牲品、违背完整性约束、应用代码显式执行 \`ROLLBACK\` | **否（系统全自动处理）** | 系统自动 **UNDO（回滚）** | 仅影响当前单个事务，不影响其他事务与数据库系统运行。DBMS 自动撤销该事务所做的全部修改。 |
| 系统故障（又称 **软故障**） | 突然停电断电、操作系统崩溃 (OS Crash)、DBMS 进程异常退出、CPU/内存硬件临时故障 | **否（重启后系统自动处理）** | 日志自动 **REDO + UNDO** | 影响正在运行的所有事务，导致内存缓冲区内容全部丢失，但外存（磁盘）数据完好。重启后系统读取日志自动恢复。 |
| 介质故障（又称 **硬故障**） | 存储磁盘物理损坏、磁头刮花、强磁场导致介质损毁、存储阵列灾难性掉线 | **是（必须人工介入！）** | **换硬盘 + 人工装载备份与重做日志** | 物理外存上的数据直接损毁！必须由 DBA 介入：更换物理硬件盘，装载最近的完全备份/差量备份，再顺序重做后续所有日志。 |
| 计算机病毒 / 人为破坏 | 黑客攻击恶意删库、人为手抖误执行 \`DROP TABLE\` / 误清空核心表 | **是（必须人工介入！）** | **制定应急策略 + 人工按时间点还原** | 属于逻辑破坏，无法由系统自动感知回滚。必须由 DBA 人工定位误操作时间戳，利用历史备份与日志执行 **时间点还原 (Point-in-Time Recovery)**。 |

#### 深入考点辨析：预期的事务内部故障 vs 非预期的事务内部故障（必考重点）

在事务内部故障中，根据故障是否能被应用程序提前预见与主动处理，细分为两大类别：

| 分类 | 核心定义 | 典型场景 | 处理方式 | 核心题眼与控制权归属 |
| :--- | :--- | :--- | :--- | :--- |
| **预期的事务内部故障** | 应用程序**能预见到的业务逻辑异常**，并在代码中显式编写了应对逻辑 | • 银行转账时**余额不足**<br>• 电商下单时**库存不足** | **由程序员通过业务代码主动触发 \`ROLLBACK\`（回滚事务）并提示用户** | 控制权在**程序员/应用程序**手中，业务代码主动捕获并调用 ROLLBACK 回滚。 |
| **非预期的事务内部故障** | 应用程序**无法提前预判或自身无法处理**的运行期底层错误 | • 并发事务**死锁**（系统选为牺牲品强行终止）<br>• **运算溢出**（如除以零、数值溢出）<br>• **违反完整性约束**（如主键冲突、外键非法） | **事务自身无法自救，必须由 DBMS（数据库管理系统）底层主动介入并强制回滚** | 控制权在 **DBMS 底层内核**，应用程序无法自救，系统自动接管强行做 UNDO。 |

---

### 五、 日志文件的核心作用：不同恢复场景下的日志必要性对比（必考真题矩阵）

日志文件在数据库系统的故障恢复中起着决定性作用。但**并非所有恢复场景都必须使用日志文件**，考场常通过表格或选择题考察不同场景对日志文件的依赖程度与根本原因：

| 恢复场景 | 是否必须使用日志文件？ | 核心原因与底层机理（考点原话必背） |
| :--- | :---: | :--- |
| **事务故障恢复** | **必须** | 事务执行非正常中断，**需要查日志逆向做 Undo（撤销）**，恢复更新前的数据状态。 |
| **系统故障恢复**<br>*(软故障 / 断电)* | **必须** | 内存脏页未刷盘或未提交事务污染磁盘，**需要查日志做 Undo（撤销未提交事务）+ Redo（重做已提交事务）**。 |
| **介质故障**<br>*(基于【动态转储】副本)* | **必须** | 备份转储过程中**存在并发事务写入**，副本数据处于**不一致状态**，**必须依靠日志修平**（重做已提交、撤销未提交）才能恢复到某一确定的一致性状态。 |
| **介质故障**<br>*(基于【静态转储】副本)* | **非必须（可不用）** | 静态转储时系统已被挂起（无事务并发运行），转储得到的数据**天然处于完全一致状态**，**直接倒回副本即可恢复到转储点**（若无需恢复转储点之后的新数据，则可不依赖日志）。 |

#### 考场命门题眼速记：
- 题干问：“**在介质故障恢复中，哪种转储方式下恢复数据可以不依赖日志文件？**” ➔ 秒选 **静态转储**！
- 题干问：“**动态转储与静态转储相比，为什么动态转储必须同时建立日志文件？**” ➔ 核心题眼：“**因为动态转储时数据处于不一致状态，必须用日志文件进行恢复和修平**”。

---

### 六、 考场核心判定题眼归纳：
1. 问：“**哪类故障必须由数据库管理员（DBA）人工介入？**”
   - **必选**：**介质故障（硬故障）**、**计算机病毒 / 人为破坏**。
2. 问：“**系统重启后自动由 DBMS 执行恢复，无需人工干预的故障是？**”
   - **必选**：**系统故障（软故障）**、**事务内部故障**。
3. 问：“**数据库发生软故障后，系统需要执行什么操作？**”
   - **必选**：**UNDO 和 REDO 操作**（对未提交事务 UNDO，对已提交事务 REDO）。`
    },
    {
      id: 'system-databases',
      title: '3. SQL Server 四大系统数据库管理策略',
      tag: '系统运维',
      highlightedKeywords: [
        'master',
        'msdb',
        'model',
        'tempdb',
        '不能备份'
      ],
      content: `### SQL Server 4 大预置系统数据库对比

在选择题和填空题中，判断四个系统数据库的特征是送分考点：

| 数据库名称 | 核心职能与存放内容 | 备份与恢复策略 | 备份支持特性（必背八股） |
| :--- | :--- | :--- | :--- |
| **master** | 记录 SQL Server 的**所有系统级信息**（实例配置、系统表、登录账号、所有用户数据库的存储路径） | **必须定期完全备份**！若损坏，整个 SQL Server 服务无法启动 | **只能做完全备份**（不支持事务日志备份，因为是简单恢复模式） |
| **msdb** | 供 **SQL Server Agent（代理服务）** 使用，保存作业调度、警报、操作员配置及备份恢复历史日志 | 建议定期备份 | 支持完全、差异、日志备份 |
| **model** | 作为在 SQL Server 实例上创建**所有新用户数据库的模板**。对 model 的修改会影响之后创建的所有新库 | 建议备份（若损坏无法创建新数据库） | 支持完全、差异、日志备份 |
| **tempdb** | 保存临时用户对象（#临时表）、内部工作表和游标中间结果。**每次服务重启都会清空并重新创建！** | **绝对不能进行备份（BACKUP）或还原！** 系统禁止对 tempdb 进行备份 | **绝对不能备份！也不需要备份**（每次重启服务自动清空重建） |

#### 考场一句话避坑：
- 题干问“**以下哪一个系统数据库绝对不能执行备份操作？**” → 秒选 **tempdb**！
- 题干问“**哪一个数据库作为用户新数据库的模版？**” → 秒选 **model**！`
    },
    {
      id: 'detach-and-attach-database',
      title: '4. SQL Server 分离数据库与附加数据库（核心机理与运行约束）',
      tag: '高频运维考点',
      highlightedKeywords: [
        '分离数据库',
        '附加数据库',
        '不删除数据文件和日志文件',
        '不受管理',
        '服务应处于启动状态',
        '不能分离活动状态'
      ],
      content: `### SQL Server 分离与附加数据库核心机理精讲

在全国计算机等级考试三级数据库技术中，**分离数据库（Detach Database）** 与 **附加数据库（Attach Database）** 是高频常考的数据库物理维护与迁移考点，常以单选题、综合判断题和填空题形式出现。

---

### 一、 分离数据库（Detach Database）的本质与目的

1. **核心定义（考点原文必背）**：
   - **分离数据库** 是指**将数据库从 SQL Server 实例中删除，但不删除数据库的数据文件（.mdf / .ndf）和日志文件（.ldf）**。
   - 分离操作仅从系统字典（如 \`master\` 系统库）中注销该数据库的元数据信息与注册映射关系。

2. **核心机理与作用**：
   - 分离数据库的实际目的，就是**让数据库的物理文件解除锁定，不受数据库管理系统（DBMS）的管理与控制**。
   - 当文件解除锁定后，用户可以**安全地将数据库的数据文件和日志文件复制或移动到另一台计算机上，或者同一台计算机的其他磁盘目录**中，实现快速数据迁移、脱机归档或冷备份。

> ⚠️ **与 DROP DATABASE 的本质区别**：
> - \`DROP DATABASE\`（删除数据库）：彻底从实例中抹除数据库，**并同时从物理磁盘上永久物理删除**所有关联的数据文件和日志文件！不可逆！
> - \`分离数据库\`：仅解除实例对物理文件的控制与关联，**磁盘上的数据文件与日志文件完好无损保留**。

---

### 二、 附加数据库（Attach Database）的本质与灵活性

1. **核心定义**：
   - **附加数据库** 是指**将先前已分离的物理数据库文件重新附加并注册到数据库管理系统中**。

2. **跨实例与跨服务器灵活性**：
   - 附加时具有极高的自由度：
     - 可以附加到**本机的另一个 SQL Server 实例**上；
     - 也可以复制到网络环境中的**另一台全新数据库服务器**并附加使用；
     - 也可以重新附加回原本的实例中。

---

### 三、 考场两大核心约束与高频命题陷阱（必背题眼）

在选择题和判断题中，关于分离与附加操作的前提条件是最容易失分的陷阱点：

| 约束维度 | 官方标准规则（正确选项） | 常见干扰项（必错陷阱） | 考点命题深度剖析 |
| :--- | :--- | :--- | :--- |
| **SQL Server 服务运行状态** | **服务应处于【启动】状态** | “必须先停止 SQL Server 服务” | **高频陷阱**！分离和附加是 DBMS 自身提供的内部管理操作，**SQL Server 服务必须正常处于启动运行状态**（停止服务属于直接去操作系统底层拷贝文件，绝非分离/附加操作）。 |
| **数据库的当前连接状态** | **不能分离连接为【活动状态】的数据库** | “活动状态的数据库可以直接强制分离” | 数据库如果存在未断开的用户会话、正在运行的活动事务或连接，系统会拒绝分离。**必须先切断所有活动连接**（例如将数据库转为单用户模式 \`SINGLE_USER\`），才能成功分离。 |
| **物理文件完整性** | 数据文件与日志文件必须匹配完整 | 只需要数据文件，日志文件随意 | 主数据文件（.mdf）是必须的，分离前保证一致性，附加时可重新定位路径。 |

---

### 四、 考场速记四句真言

1. **“分离只除名，不删物理盘”**：从实例中注销数据库，但绝不删除数据文件与日志文件。
2. **“脱离管辖权，自由去拷贝”**：让文件不受 DBMS 管理，以便复制到其他机器或路径。
3. **“服务必须开，停机是大错”**：分离与附加数据库时，SQL Server 服务必须处于启动状态。
4. **“活动不能拆，断开再分离”**：处于活动连接状态的数据库严禁直接分离，必须先切断所有活跃会话。`
    },
    {
      id: 'database-mirroring-modes',
      title: '5. SQL Server 数据库镜像的三大模式（高可用 / 高保护 / 高性能对比矩阵与架构角色）',
      tag: '高可靠容灾',
      highlightedKeywords: [
        '数据库镜像',
        '主体服务器',
        '镜像服务器',
        '见证服务器',
        '高可用模式',
        '高保护模式',
        '高性能模式',
        '同步传输',
        '异步传输',
        '自动故障转移',
        '手工故障转移',
        '强制服务',
        '零数据丢失'
      ],
      content: `### SQL Server 数据库镜像（Database Mirroring）核心机理精讲

> 📌 **官方考纲必背原话（数据库镜像三大优点与成本代价）**：
> **数据库镜像是一种简单的策略，具有下列优点：**
> 1. **增强数据保护功能**。数据库镜像提供完整或接近完整的数据冗余，具体取决于运行模式是高安全性模式还是高性能模式；
> 2. **提高数据库的可用性**。发生灾难时，在具有自动故障转移功能的高安全性模式下，自动故障转移可快速使数据库的备用副本在线(而不会丢失数据)。在其他运行模式下，数据库管理员可以选择强制服务(可能丢失数据)，以替代数据库的备用副本；
> 3. **提高生产数据库在升级期间的可用性**。
>
> ⚠️ **成本代价**：**因为数据库镜像技术需要额外存放数据的空间，所以会增加应用成本。**

在 SQL Server 高可用性与容灾技术中，**数据库镜像（Database Mirroring）** 是一项基于数据库级别的热备解决方案。它通过将主体数据库的**事务日志记录**实时或准实时传输到镜像数据库服务器，并在镜像库中重做这些日志，从而维护两个完全独立且内容一致的数据库副本。

---

### 一、 数据库镜像系统的三大核心服务器角色

数据库镜像系统最多涉及三台服务器实例：

1. **主体服务器（Principal Server）**：
   - 承载当前生产业务的核心数据库（主体数据库），对外提供正常的客户端读写连接与服务；
   - 负责将本地事务日志记录发送给镜像服务器。
2. **镜像服务器（Mirror Server）**：
   - 存放主体数据库的副本（镜像数据库），处于恢复中（RESTORING）状态，不直接接收客户端读写；
   - 接收主体发来的日志并按顺序重做，以保持与主体数据库的同步。
3. **见证服务器（Witness Server，可选角色）**：
   - **仅用于“高可用模式”**，是可选但实现自动切换的唯一仲裁者；
   - **见证服务器不包含任何数据库副本，也不处理任何业务数据**！它的唯一职责是与主体和镜像服务器互相发送心跳包，形成“三方仲裁（Quorum）”，当主体服务器失联时，协同镜像服务器发起**自动故障转移**。

---

### 二、 数据库镜像的三大模式全景对比矩阵（必背核心表格）

SQL Server 数据库镜像根据安全级别（事务同步方式）以及是否配置见证服务器，严格划分为以下三种运行模式：

| 镜像模式 | 事务传输方式 | 是否需要见证服务器？ | 故障转移方式（容灾切换） | 核心目标与特性 |
| :--- | :--- | :---: | :--- | :--- |
| **高可用模式**<br>*(High Availability)* | **同步传输**<br>*(两边都写日志成功才提交)* | **必须有见证服务器** | **自动故障转移**<br>*(系统全自动切机，无需人工干预)* | **零数据丢失 + 业务不中断**。<br>主体一旦宕机，镜像服务器自动升为主体。 |
| **高保护模式**<br>*(High Protection)* | **同步传输**<br>*(两边都写日志成功才提交)* | **无见证服务器**<br>*(不需要 / 无见证)* | **手工故障转移**<br>*(需管理员人工介入执行切换)* | **零数据丢失，但需人工介入切机**。<br>保证绝对的数据安全性，容忍切机时的短暂人工响应时间。 |
| **高性能模式**<br>*(High Performance)* | **异步传输**<br>*(主体写完日志即返回客户端)* | **无见证服务器**<br>*(只能由主体+镜像两台组成)* | **仅支持强制手工故障转移**<br>*(强制服务，可能丢失少量未同步数据)* | **追求极致吞吐与响应速度**。<br>摆脱网络往返延迟对客户端事务响应时间的强约束。 |

---

### 三、 三大模式深度原理解析与考点题眼

#### 1. 高可用模式 (High Availability Mode)
- **运行机理**：运行在**高安全级别（Full Safety）**下，事务采用**同步传输**机制——客户端发出 COMMIT 后，主体必须等镜像服务器确认已将日志写入磁盘后，才向客户端返回事务提交成功。
- **仲裁机制**：主体服务器、镜像服务器和见证服务器构成“三方会谈”，任何两方保持连通即可形成**多数派（仲裁裁决 Quorum）**。若主体宕机，镜像与见证判定主体死亡，镜像服务器秒级自动提升为新的主体服务器。
- **考场题眼**：题干中只要出现**“自动故障转移”**、**“零数据丢失”**或**“配置了见证服务器”**，答案必为 **高可用模式**！

#### 2. 高保护模式 (High Protection Mode)
- **运行机理**：同样运行在**高安全级别（Full Safety）**下，事务采用**同步传输**，确保镜像服务器的数据绝对与主体完全同步（RPO = 0，零数据丢失）。
- **与高可用模式的区别**：**没有配置见证服务器**。因此当主体服务器意外崩溃时，镜像服务器无法单方面确信主体是否真正宕机（防止脑裂 Split-Brain），所以**系统无法进行自动故障转移**，必须由 DBA 手动执行故障转移命令（\`ALTER DATABASE ... SET PARTNER FAILOVER\`）完成切机。
- **考场题眼**：题干要求“**保证零数据丢失，但不需要/未配置见证服务器**” ➔ 秒选 **高保护模式**！

#### 3. 高性能模式 (High Performance Mode)
- **运行机理**：运行在**低安全级别（Off Safety）**下，事务采用**异步传输**机制。主体服务器写完本地事务日志后立即向客户端反馈提交成功，无须等待镜像服务器应答。
- **优缺点**：网络延迟对数据库吞吐量几乎没有负面影响，性能最高；但如果主体突然断电或损毁，可能存在若干尚未传输到镜像的事务日志，切机时属于**强制服务（Forced Service）**，可能会**丢失少量未同步数据**。
- **考场题眼**：题干出现“**异步传输**”、“**追求极致性能**”或“**可能丢失少量数据**” ➔ 秒选 **高性能模式**！

---

### 四、 考场秒杀口诀与避坑指南

1. **“两同一下高可用，自动切换见证灵”**：
   - 同步传输 + 有见证服务器 = **高可用模式**（自动故障转移）。
2. **“同步无见证，手工高保护”**：
   - 同步传输 + 无见证服务器 = **高保护模式**（手工故障转移，零数据丢失）。
3. **“异步高性能，强制可能丢”**：
   - 异步传输 + 无见证服务器 = **高性能模式**（仅支持强制手动故障转移，可能丢失少量数据）。
4. **见证服务器送分题**：
   - 见证服务器**不参与数据处理**，**不保存数据库副本**，仅参与仲裁裁决！`
    }
  ]
};
