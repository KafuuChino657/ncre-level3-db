import { Topic } from '../../types';

export const strategyTopic: Topic = {
  id: 'strategy',
  title: '考试大纲与答题策略',
  badge: '通关指导',
  icon: 'Target',
  description: '全国计算机三级数据库技术题型分布、分值策略与单选核心秒杀口诀',
  subTopics: [
    {
      id: 'score-distribution',
      title: '一、题型分值与拿分策略',
      tag: '分值大纲',
      highlightedKeywords: ['40分', '30分', '60分及格', '单选题', '填空题', '应用题'],
      content: `### 题型分布与通关标准

全国计算机等级考试（NCRE）三级数据库技术满分为 **100分**，**60分及格**。考试时长为 **120分钟**，纯机考操作。

| 题型 | 题目数量 | 每题分值 | 总分值 | 目标得分 | 备考策略 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **单选题** | 40道 | 1分 | **40分** | 32+分 | 考点覆盖面广，重在概念辨析与口诀秒杀，每题控制在30秒内完成 |
| **填空题** | 15空 (或20空) | 2分/1.5分 | **30分** | 22+分 | **失分重灾区**！考查精确术语、SQL代码关键字、系统库名，必须一字不差 |
| **应用题/设计综合题** | 2~3道大题 | 小问累计 | **30分** | 20+分 | 集中在E-R转关系模式、死锁分析、索引设计、触发器/游标SQL编写 |

#### 答题时间分配黄金法则
1. **前30分钟**：扫完40道单选。遇到不确定的先选第一直觉并标记，绝不要在一道1分的选择题上纠结超过2分钟。
2. **中间40分钟**：攻克填空题。注意英文单词大小写（如 SQL 关键字建议大写，列名与题干一致）。
3. **后40分钟**：书写应用设计大题，仔细核对主外键命名、数据类型、语法闭合括号。
4. **最后10分钟**：检查检查是否有漏填、空白题目，确保机考答题卡所有题目已交卷保存。`
    },
    {
      id: 'quick-kill-tips',
      title: '二、三级单选题“秒杀口诀”全集',
      tag: '秒杀口诀',
      highlightedKeywords: [
        '事务概要设计选逻辑设计', 
        '稀疏必定是聚集', 
        '大事务并发度下降死锁上升', 
        '三级模式两级映射保证独立性',
        'WAL日志先写原则',
        '两段锁是可串行化的充分条件',
        '系统吞吐量',
        '响应时间',
        '并发访问量',
        '资源利用率',
        'RETURNS',
        'RETURN',
        'dbo.',
        '三大可编程对象',
        '概念结构设计',
        '关系模式规范化',
        '系统总体框架设计',
        '事务概要设计',
        '事务详细设计',
        '实施阶段',
        '装入初始数据',
        '系统试运行',
        '数据分配方式',
        '集中式',
        '分割式',
        '全复制式',
        '混合式',
        'ODS',
        'ODS Ⅰ',
        'ODS Ⅱ',
        'ODS Ⅲ',
        'ODS Ⅳ',
        '内联表值函数',
        '多语句表值函数',
        'RETURNS TABLE',
        '可以传参的视图',
        'DELETED 表',
        'INSERTED 表',
        'RAID 0',
        'RAID 1',
        'RAID 5',
        'RAID 6',
        'RAID 10',
        '知识发现 (KDD)',
        '数据准备',
        'DROP FUNCTION',
        'DROP VIEW',
        'DROP TRIGGER',
        'DROP PROCEDURE',
        'DROP INDEX',
        '以记录为单位的日志文件',
        '以数据块为单位的日志文件',
        '主要数据文件',
        '次要数据文件',
        '事务日志文件',
        '主文件组',
        '自主存取控制',
        'DAC',
        '强制存取控制',
        'MAC',
        '许可证级别',
        '客体的密级',
        '下读',
        '同级写',
        '特洛伊木马',
        '数据库重组',
        '数据库重构',
        '快照',
        '快照方式',
        '增量方式',
        '数据库快照',
        '写时复制',
        '六层模式结构',
        '全局概念模式',
        '分片模式',
        '分配模式',
        '分片透明性',
        '位置透明性',
        '局部数据模型透明性',
        'FETCH',
        'NEXT',
        'PRIOR',
        'FIRST',
        'LAST',
        'ABSOLUTE',
        'RELATIVE',
        'BACKUP DATABASE',
        'WITH DIFFERENTIAL',
        'BACKUP LOG',
        'RESTORE LOG',
        '水平分片',
        '垂直分片',
        '导出分片',
        '导出水平分片',
        '混合分片',
        '结果是不相同的',
        '数据建模',
        '三级模型',
        '概念模型',
        '逻辑模型',
        '物理模型'
      ],
      content: `### 考场秒杀口诀库（题眼对应直选法则）

在做单选题时，遇到特定提法直接秒杀对应选项，正确率高达98%以上：

1. **题目提问阶段任务**：
   - 看到「**事务概要设计**」或「**关系模式规范化**」 → 秒选 **逻辑设计阶段**（绝不是概念设计！概念设计只管E-R图和抽象事务）。
   - 看到「**存取路径**、**存储结构**、**索引建立**、**分区策略**」 → 秒选 **物理设计阶段**。
   - 看到「**数据字典**、**数据流图(DFD)**、**需求说明书**」 → 秒选 **需求分析阶段**。

2. **索引与存储做题铁律**：
   - 看到「**稀疏索引**」 → 其索引项对应物理块，数据文件**必须是有序的**，故**稀疏索引一定是聚集索引**！
   - 看到「**非聚集索引**」 → 绝不可能是稀疏索引，**非聚集索引只能是稠密索引**！
   - 一张数据表**最多只能建 1 个聚集索引**，但可以建立**多个非聚集索引**。

3. **并发、事务与死锁铁律**：
   - 看到「**大事务**」的影响 → 占用锁时间长，导致**系统并发度下降**、**发生死锁概率上升**。
   - 看到「**两段锁协议(2PL)**」 → 两段锁是保证并发调度**可串行化的充分条件**，但**不是必要条件**；两段锁**不能避免死锁**（可能发生死锁）！
   - 看到「**活锁**」 → 采用**先来先服务(FCFS)**策略即可避免活锁。

4. **系统库与数据维护铁律**：
   - 看到「**记录SQL Server所有系统级配置**」 → 秒选 **master**。
   - 看到「**保存警报、计划作业、调度任务**」 → 秒选 **msdb**。
   - 看到「**新创建用户数据库的模板**」 → 秒选 **model**。
   - 看到「**临时表、工作表，每次重启重建**」 → 秒选 **tempdb**（且 **tempdb 绝不能进行备份**！）。

5. **高级数仓与挖掘铁律**：
   - 看到「**面向主题、集成的、不可更新的(非易失)、随时间变化的**」 → 秒选 **数据仓库(DW)**。
   - 看到「**基于关系数据库的OLAP / 星型或雪花模式**」 → 秒选/填 **ROLAP**。
   - 看到「**基于多维数组的OLAP / 预计算数据立方体Cube**」 → 秒选/填 **MOLAP**。
   - 看到「**混合型OLAP / 高层多维底层关系**」 → 秒选/填 **HOLAP**。
   - 看到「**下次 / 未来走势 / 随时间变化规律 / 滞后周期**」 → 秒选 **时间序列模式**。
   - 看到「**购物篮 / 同时购买 / 同时发生**」 → 秒选 **关联规则模式**（对应 **Apriori 算法**、**支持度与置信度**）。
   - 看到「**客户细分 / 分成不同类别 / 群体画像**」 → 秒选 **分类 / 聚类模式**（无监督聚类对应 **K-means 算法**）。

6. **DBAS 性能指标秒杀铁律**：
   - 看到「**单位时间内可以完成的数据库事务数量**」 → 秒选/填 **系统吞吐量**（TPS/QPS）。
   - 看到「**从用户发出请求到返回结果所需的时间**」 → 秒选/填 **响应时间**（秒/毫秒）。
   - 看到「**同一时刻能够支持并处理的并发用户数/会话数**」 → 秒选/填 **并发访问量**。
   - 看到「**硬件资源的使用程度 / 占用百分比**」 → 秒选/填 **资源利用率**（%）。

7. **T-SQL 核心编程三大对象与填空铁律**：
   - **三大可编程对象秒杀区分**：
     - **标量函数**（\`CREATE FUNCTION ... RETURNS 类型\`）：头部定义类型必填 **\`RETURNS\`**（带 S），尾部返回结果必填 **\`RETURN\`**（不带 S），调用时**必加 \`dbo.\` 架构名前缀**；题干特征词：“**创建标量函数**”、“**统计并返回一个值**”。
     - **存储过程**（\`CREATE PROCEDURE ...\`）：**不带 RETURNS**；定义参数与 \`EXEC\` 调用传参**两头都必须写 \`OUTPUT\`**；单语句赋值时可省略 \`BEGIN...END\`；题干特征词：“**创建存储过程**”、“**执行业务处理**”。
     - **触发器**（\`CREATE TRIGGER ... ON 表 FOR/AFTER/INSTEAD OF\`）：**无参数、无返回值**；题干特征词：“**当向表中插入/修改时自动触发**”。
   - **并列名次查询**：看到「**前 n 名包含并列**」 → 必填 **\`TOP n WITH TIES\`**，且**必须与 \`ORDER BY\` 联合使用**！

8. **DBAS 阶段推进与数据/程序双线并行铁律**：
   - **概念设计阶段**：数据层面看 **概念结构设计（E-R 图）**，行为/程序层面看 **系统总体框架设计**。
   - **逻辑设计阶段**：数据层面看 **关系模式规范化（转成表与列）**，行为/程序层面看 **事务概要设计、应用程序概要设计**。
   - **物理设计阶段**：数据层面看 **存取方法、索引设计、分区、存储空间配置**，行为/程序层面看 **事务详细设计、应用程序详细设计**。

9. **实施阶段（Implementation）四件事铁律**：
   - 看到「**建库建表（执行 DDL）**」、「**写代码与联调**」、「**装入初始数据（上线前）**」、「**系统试运行/功能测试**」中的任意一项或组合 → 秒选/填 **实施阶段（实现与部署 Implementation）**（绝对不能选成系统设计或运维阶段）！

10. **分布式数据库四大数据分配方式铁律**：
   - 看到「**所有数据片段都安排在同一个场地上**」 → 秒选/填 **集中式**。
   - 看到「**所有数据只有一份，分割成若干逻辑片段指派在特定场地**」 → 秒选/填 **分割式**（零冗余）。
   - 看到「**数据在每个场地重复存储 / 每个场地都有完整的数据副本**」 → 秒选/填 **全复制式**（读取性能与高可用性最高，更新一致性代价最大）。
   - 看到「**每个子集安排在一个或多个不同场地，介于分割式和全复制式之间**」 → 秒选/填 **混合式**。

11. **ODS 四类分类与考场真假辨析铁律**：
   - 题眼定位：ODS 是介于业务系统（OLTP）和数据仓库（DW）之间的**缓冲层**。
   - 口诀：**Ⅰ 秒、Ⅱ 时、Ⅲ 天、Ⅳ 双向！**
   - 辨析真伪陷阱：
     - **ODS Ⅰ（秒级/准实时）**：看到题干说“ODS Ⅰ 为天级” → **必错**！
     - **ODS Ⅱ（小时级）**：周期性几小时刷新一次 → **正确**！
     - **ODS Ⅲ（天级/批处理）**：看到题干说“ODS Ⅲ 为秒级同步” → **必错**！
     - **ODS Ⅳ（双向流动）**：不仅接收上游 OLTP 数据，还接收下游数据仓库反馈的报表决策数据 → **正确**！

12. **数据库管理员（DBA）核心职责铁律**：
   - **官方真题必背原话**：
     数据库管理员的职责有：
     1. **数据库的转储和恢复**；
     2. **数据库安全性、完整性控制**；
     3. **数据库性能的检测和改善**；
     4. **数据库的重组和重构**。

13. **数据库镜像三大优点与成本代价铁律**：
   - **官方真题必背原话**：
     数据库镜像是一种简单的策略，具有下列优点：
     1. **增强数据保护功能**。数据库镜像提供完整或接近完整的数据冗余，具体取决于运行模式是高安全性模式还是高性能模式；
     2. **提高数据库的可用性**。发生灾难时，在具有自动故障转移功能的高安全性模式下，自动故障转移可快速使数据库的备用副本在线(而不会丢失数据)。在其他运行模式下，数据库管理员可以选择强制服务(可能丢失数据)，以替代数据库的备用副本；
     3. **提高生产数据库在升级期间的可用性**。
   - **成本代价**：**因为数据库镜像技术需要额外存放数据的空间，所以会增加应用成本。**

14. **内联表值函数（可以传参的视图）考场秒杀铁律**：
   - 核心本质：“**可以传参的视图**”。
   - 语法“三无”铁律：
     - **无表变量名**：只写 \`RETURNS TABLE\`（绝无 \`@t\`）。
     - **无列结构定义**：不定义字段清单，由内部 SELECT 自动推导。
     - **无 BEGIN...END**：主体仅一条 \`RETURN (SELECT ...);\`，**严禁写 \`BEGIN...END\`**，无需 \`INSERT INTO\` 填充。
   - 对比多语句表值函数：多语句表值函数有 \`@t TABLE (列定义)\`，且**必须有 \`BEGIN...END\` 与 \`INSERT INTO\`**。
   - 调用位置铁律：表值函数返回表，必须在 **FROM** 子句中调用（\`SELECT * FROM f_get_dept_emp(10)\`）。

15. **触发器的两张特殊临时表（DELETED 表与 INSERTED 表）考场秒杀铁律**：
   - **两张特殊临时表对照矩阵**：
     - **INSERT（插入）**：DELETED 表为 **（无 / 空）**；INSERTED 表 **存放新插入的数据行**。
     - **DELETE（删除）**：DELETED 表 **存放刚刚被删除的旧数据行**；INSERTED 表为 **（无 / 空）**。
     - **UPDATE（修改）（本题考点）**：DELETED 表 **存放修改前（更新前）的旧值**；INSERTED 表 **存放修改后（更新后）的新值**。
   - **秒杀记忆口诀**：
     - **D 存旧（Delete/Old），I 存新（Insert/New）**；
     - **改时两表全：旧去 DELETED，新入 INSERTED！**

16. **常用 RAID 阵列（RAID 0/1/5/6/10）容量与容错秒杀铁律**：
   - **盘数与容量极速心算**（设 $N$ 块盘，单盘容量 $C$）：
     - **RAID 0**：最少 **2 块**；容量 **$N \\times C$**；容错 **0 块**（无冗余，坏一全丢）；场景：测试、临时库。
     - **RAID 1**：最少 **2 块**（偶数）；容量 **$1 \\times C$**（50%）；容错 **1 块**；场景：**关键事务日志文件（Log Files）**。
     - **RAID 5**：最少 **3 块**；容量 **$(N - 1) \\times C$**；容错 **1 块**；写惩罚明显；场景：读多写少、数据仓库（OLAP）。
     - **RAID 6**：最少 **4 块**；容量 **$(N - 2) \\times C$**；容错 **2 块**（双重奇偶校验）；场景：冷数据归档。
     - **RAID 10**：最少 **4 块**（偶数）；容量 **$(N / 2) \\times C$**（50%）；容错：每组镜像坏 1 块（极限坏 $N/2$ 块）；场景：**核心 OLTP 业务库主力（高并发读写）**。

17. **知识发现（KDD）三大阶段与数据准备细分步骤秒杀铁律**：
   - **三大阶段流向**：
     - \`1. 数据准备 (Data Preparation) ──> 2. 数据挖掘 (Data Mining) ──> 3. 结果的解释评估 (Interpretation & Evaluation)\`
   - **第一阶段：数据准备（细分三步法，极高频考点）**：
     - **数据选择（Selection）**：按目标选业务子集。
     - **数据预处理/清洗（Preprocessing/Cleaning）**：处理缺失值、去异常噪声。
     - **数据转换（Transformation）**：归一化、降维、离散化，转换成模型格式。
   - **做题秒杀**：看到“**挑选子集、清洗缺失值、降维归一化**” ➔ 必选【**数据准备阶段**】！

18. **“删除/创建......使用 ______ 语句”考场定式秒杀铁律**：
   - **“删除......使用 ______ 语句”五大定式填空**：
     - **删函数** ➔ 填 \`DROP FUNCTION\`
     - **删视图** ➔ 填 \`DROP VIEW\`
     - **删触发器** ➔ 填 \`DROP TRIGGER\`
     - **删存储过程** ➔ 填 \`DROP PROCEDURE\`（或 \`DROP PROC\`）
     - **删索引** ➔ 填 \`DROP INDEX\`
   - **“创建......使用 ______ 语句”同理对应**：
     - **建函数** ➔ \`CREATE FUNCTION\`
     - **建视图** ➔ \`CREATE VIEW\`
     - **建触发器** ➔ \`CREATE TRIGGER\`
     - **建存储过程** ➔ \`CREATE PROCEDURE\`（或 \`CREATE PROC\`）
     - **建索引** ➔ \`CREATE INDEX\`
     - **建表** ➔ \`CREATE TABLE\`

19. **官方教材两种日志文件格式秒杀铁律**：
   - **以记录为单位（Record-level）**：粒度细，记录 BEGIN/COMMIT 及每一次具体更新的旧值/前像与新值/后像。
   - **以数据块为单位（Block-level · 本题考点）**：粒度粗，记录更新前后整个物理数据块的**镜像（Before/After Image）**，恢复直接块覆盖。
   - **秒杀题眼**：看到“**被更新数据块在更新前后的整个物理块镜像**” ➔ 必选【**以数据块为单位的日志文件**】！

20. **数据库文件类型（.mdf / .ndf / .ldf）与文件组秒杀铁律**：
   - **主数据文件（.mdf）**：**有且仅有 1 个**，存放系统信息，**必须位于主文件组（PRIMARY）中**！
   - **次要数据文件（.ndf）**：**0 到多个**，用于跨盘负载均衡，未显式指定时默认归入主文件组。
   - **事务日志文件（.ldf）**：**至少 1 个**，**绝不属于任何文件组**！
   - **高频避坑**：选项说“事务日志文件属于主文件组” ➔ **直接判定为错误！**

21. **自主存取控制（DAC）与强制存取控制（MAC）秒杀铁律**：
   - **强制存取控制两大核心规则（必背原话）**：
     - **读规则（下读）**：**仅当主体的许可证级别大于或等于客体的密级时，主体才能读取相应的客体**（$S_{level} \ge O_{level}$）。
     - **写规则（同级写）**：**仅当主体的许可证级别等于客体的密级时，主体才能写相应的客体**（$S_{level} = O_{level}$）。
   - **DAC vs MAC 对比核心速记**：
     - **控制主体**：DAC 用户自主控制；MAC 系统管理员/安全策略强制控制。
     - **安全性与威胁**：DAC 较灵活但**易受特洛伊木马恶意程序攻击**（C1/C2 级）；MAC 极高**防止信息泄露抗木马**（B1 级以上）。
     - **实现手段**：DAC 靠 \`GRANT\`、\`REVOKE\`、\`DENY\` 语句；MAC 靠**安全密级标签**（绝密、机密、秘密、公开）。
     - **判定规则**：DAC 查权限表；MAC 遵循上下读写规则（下读、同级写）。

22. **“数据库重组” vs “数据库重构” 秒杀铁律**：
   - **数据库重组（Reorganization）**：“大扫除、理碎纸”，**绝不修改模式与内模式**，做的是整理磁盘碎片、重建索引、回收空闲空间、清理溢出区，**对应用程序完全透明（一行代码都不用改）**。
   - **数据库重构（Restructuring）**：“大装修、改户型”，**会修改模式或内模式**，做的是垂直/水平分表、增加新字段、修改数据类型、调整规范化级别，**可能需要修改视图甚至重写前端业务 SQL**。
   - **秒杀口诀**：**“整理碎片看重组（不伤筋动骨），表构变更选重构（伤筋动骨）！”**

23. **数仓抽取方式（快照 vs 增量）与数据库快照（写时复制）秒杀铁律**：
   - **快照方式（全量抽取）**：每次抽取都将源数据全部读取并覆盖目标表；用于**变化频率低、数据量相对稳定的维度数据**。
   - **增量方式（增量抽取）**：每次只抽取变化量；用于**数据量极大、频繁变动的流水事实数据**（如交易记录、日志）。
   - **数据库快照与写时复制**：SQL Server 数据库快照是源库的一个**只读的静态视图**，基于**写时复制（Copy-on-Write）**技术实现（仅当源库原始页首次要被修改写入前才复制保存到快照稀疏文件中，秒级生成，极省空间）。

24. **分布式数据库自顶而下六层模式与三级透明性秒杀铁律**：
   - **自顶而下严格层级顺序**：**全局外模式**（全局应用用户视图） ➔ **全局概念模式**（全体数据逻辑结构） ➔ **分片模式**（全局到片段映像） ➔ **分配模式/位置模式**（片段到物理场地映像） ➔ **局部概念模式**（具体节点物理片段逻辑结构） ➔ **局部内模式**（本地场地物理存储细节）。
   - **三级透明性精准夹层位置（必考选项判定）**：
     - **全局概念模式 与 分片模式 之间** ➔ **【分片透明性】**（最高级别，用户不知切分规则）
     - **分片模式 与 分配模式（位置模式）之间** ➔ **【位置透明性】**（中间级别，用户不知机器位置）
     - **分配模式 与 局部概念模式 之间** ➔ **【局部数据模型透明性 / 局部映像透明性】**（最低级别，用户不知本地DBMS模型）
   - **秒杀口诀**：**“外概分配局局部，概分之间是分片，分配之间是位置，配局之间是模型！”**

25. **游标 FETCH 六大定位提取动作速记与秒杀铁律**：
   - **\`FETCH FIRST\`** ➔ **首行**
   - **\`FETCH LAST\`** ➔ **尾行**
   - **\`FETCH PRIOR\`** ➔ **上一行**
   - **\`FETCH NEXT\`** ➔ **下一行**（默认选项；初次提取返回第 1 行）
   - **\`FETCH ABSOLUTE n\`** ➔ **全局绝对第 n 行**（负数表示倒数第 n 行；为 0 时**不返回行**）
   - **\`FETCH RELATIVE n\`** ➔ **从当前位置相对移动 n 行**（为 0 时**返回当前行**；初次指定 0 或负数不返回行）
   - **参数类型要求**：常量为整数，动态变量类型必须为 **\`smallint\`、\`tinyint\` 或 \`int\`**。

26. **备份与还原 T-SQL 语法开头与考场关键词秒杀铁律**：
   - **完整备份**：\`BACKUP DATABASE 库名 TO ...\`（考场关键词：完整备份、完整数据库备份）。
   - **差异备份**：\`BACKUP DATABASE 库名 TO ... WITH DIFFERENTIAL\`（考场关键词：差异备份；**核心陷阱：末尾必须显式加 \`WITH DIFFERENTIAL\`**，漏加直接退化成完整备份！）。
   - **日志备份**：\`BACKUP LOG 库名 TO ...\`（考场关键词：事务日志备份、日志备份；**核心避坑：开头是 \`BACKUP LOG\`，绝不能写成 \`BACKUP DATABASE\`！**）。
   - **还原日志**：\`RESTORE LOG 库名 FROM ...\`（考场关键词：还原事务日志；**开头必须是 \`RESTORE LOG\`**，与还原数据库的 \`RESTORE DATABASE\` 严格区分！）。

27. **分布式数据库四大分片类型秒杀铁律**：
   - **水平分片**：按条件把**全局关系的所有元组划分成若干不相交子集**（基于行选择 $\sigma$，切行）。
   - **垂直分片**：把**属性集分成若干子集并在其上作投影运算**（基于列投影 $\pi$，**每个分片必须包含主键**，切列）。
   - **导出分片（导出水平分片）**：**水平分片的条件不是本关系属性的条件，而是其他关系属性的条件**。
   - **混合分片**：以上三种方法的混合。**先水平分片再垂直分片，与先垂直分片再水平分片，他们的结果是不相同的！**（⚠️ 选项若说“结果相同”直接判错！）。
   - **秒杀口诀**：“切行元组是水平，切列投影是垂直（带主键），借用他表是导出，混合顺序结果不相同！”

28. **数据建模标准“三级模型”自顶向下秒杀铁律**：
   - **概念模型（业务层）**：面向业务与现实世界，表达实体和业务需求（如 **E-R 图**），**完全不依赖于具体软硬件和 DBMS**。
   - **逻辑模型（中间层 · 核心采分点）**：**将概念模型转化为特定的数据组织形式**（如**关系模型**、**维度模型 / 星型模型 / 雪花模型**），**定义表结构、属性字段、关联关系（主外键）**，独立于具体物理实现。
   - **物理模型（底层）**：针对**具体的软硬件配置、存储介质、分区方式、索引及物理存储参数**进行落地实现。
   - **秒杀题眼**：看到“**将概念模型转化为特定的数据组织形式，定义表结构和属性字段**” ➔ 必选/必填【**逻辑模型（逻辑设计）**】！

29. **三大恢复模式速查铁律**：
   - **简单恢复模式**：不支持日志备份，事务提交后**自动截断日志**，只能还原到完整/差异备份点（只读库/数仓/测试开发库）。
   - **完整恢复模式**：记录所有事务细节，**支持还原到任意时间点（Point-in-Time）**，日志不自动释放（**须定期做日志备份防打满硬盘**，核心 OLTP 交易库）。
   - **大容量日志恢复模式**：针对大批量操作（如 \`BULK INSERT\`）**最小化日志记录**，提升导入性能，**不保证任意精细时间点还原**（海量批处理导入临时切换）。
   - **秒杀口诀**：“自动截断选简单，时点还原选完整，批量提速大容量！”

30. **固定服务器角色与权限（sp_addsrvrolemember）**：
   - \`EXEC sp_addsrvrolemember\` 是登录账户角色，作用是**为登录账户赋角色权限**。
   - **高频秒杀对应**：
     - \`bulkadmin\` ➔ 执行 \`BULK INSERT\` 语句；
     - \`dbcreator\` ➔ 创建、修改、删除和还原数据库；
     - \`diskadmin\` ➔ 管理磁盘文件；
     - \`processadmin\` ➔ 管理在 SQL Server 实例中运行的进程；
     - \`securityadmin\` ➔ 管理服务器登录账户；
     - \`serveradmin\` ➔ 配置服务器范围的设置；
     - \`setupadmin\` ➔ 添加和删除链接服务器；
     - \`sysadmin\` ➔ 拥有最高权限，跨越所有其它固定服务器角色。

31. **三层浏览器/服务器（B/S）速杀题眼**：
   - **表示层（表现层）**：载体为**客户端浏览器（Browser）**，组件为 HTML/CSS/JavaScript、表单输入与简单前端校验，**在客户端执行**。
   - **业务逻辑层**：载体为 **Web 服务器 / 应用服务器**，负责业务规则判断、复杂计算流转与 API 调用，**在应用/Web 服务器上执行**。
   - **数据层**：载体为**数据库服务器（Database Server）**，负责数据持久化存储、存储过程、触发器（Trigger）与约束机制，**在数据库服务器上执行**。

32. **数仓元数据两大分类秒杀口诀**：
   - **技术型元数据**：面向**开发人员/DBA/运维**，管物理表结构、字段、ETL清洗映射与存储路径（**底层技术说明书**）。
   - **业务型元数据**：面向**业务分析师/管理层/决策者**，管指标口径（如活跃用户定义、GMV统计）、部门归属与保密等级（**业务翻译官**）。
   - **秒杀口诀**：“字段表构归技术，口径定义归业务；DBA 看技术说明书，分析师找业务翻译官！”

33. **DBAS 各阶段 UML 图体系与符号秒杀**：
   - **类间符号**：关联（实线）、聚集（实线+空心菱形）、组合（实线+实心菱形）、泛化/继承（实线+空心三角）、依赖（虚线+箭头）、实现/精化（虚线+空心三角）。
   - **逻辑设计映射**：类图中的**类转为表**，**关系转为外键**。
   - **阶段图眼**：
     - 系统结构图：**类图（静态）** + **顺序图、通信图（动态）**；顺序图强调**时间**，通信图强调**空间**。
     - 流程：**活动图**（可描述并行操作，流程图只能串行）。
     - 微观：**对象图**（特定时间的快照）；**时间图**（时间驱动状态转换，整个矩形框是生命线）。
     - 宏观：**包图**（高内聚低耦合）；**交互概述图**（活动图+顺序图融合）。
     - 部署：**部署图/配置图**（在集成测试前使用，说明 exe 如何部署到计算机中）。

34. **DBAS 详细设计、安全架构与实施秒杀速记**：
   - **界面设计三步法**：初步设计（**总体**）➔ 用户界面细节设计（**概要**）➔ 原型设计与改进（**详细**），采用**原型迭代法**。
   - **安全设计五大保护**：安全性（防非法使用/破坏）、完整性（正确/一致/相容）、并发性（多用户共享一致）、备份恢复（冗余重建不丢数据）、加密传输（数字证书/对称密钥/数字签名/**数字信封**）。
   - **封锁与死锁**：排它锁（X锁，写锁，仅加锁者可读写）、共享锁（S锁，读锁，他人可读不可改）；防死锁牢记**“同序访、免交互、短事务、行级锁、绑定连”**。
   - **实施阶段**：建库三因素（**初空、增量、性能**）；装载三步（**筛数据 ➔ 换格式 ➔ 输数据**）；试运行两测试（**功能测试 + 性能测试**）。

35. **存储管理器 vs 查询处理器秒杀口诀**：
   - **查询处理器（上层脑力）**：语法词法分析、代数优化、**生成物理执行计划**。
   - **存储管理器（底层体力）**：缓冲池置换（**缓冲区**）、磁盘块分配（**文件**）、权限与约束（**权限及完整性**）、加锁隔离（**事务**）、WAL 与 REDO/UNDO（**恢复**）。
   - **口诀**：“分析优化出计划找查询，管盘管页管锁记日志全归存储！”

36. **SQL Server 备份与恢复秒杀铁律**：
   - **双恢复级别**：支持 **DB 级** 和 **文件级** 恢复（文件备份仅恢复损坏文件，更灵活快捷）。
   - **恢复四步走**：**先完备 ➔ 再差备 ➔ 后日志 ➔ 库上线**（顺序绝对不可倒置）。
   - **参数题眼**：覆盖备份用 **WITH INIT**；连环还原全加 **WITH NORECOVERY**（告知系统之后还有文件需恢复）；最后上线用 **WITH RECOVERY**。

37. **数据库级固定角色权限秒杀**：
   - **写全部数据 (增删改)** ➔ **\`db_datawriter\`**
   - **加删数据库用户 (访问权)** ➔ **\`db_accessadmin\`**
   - **备份与恢复数据库** ➔ **\`db_backupoperator\`**
   - **建库对象 / 全权支配** ➔ **\`db_owner\`**

38. **SELECT ... INTO vs INSERT INTO ... SELECT 秒杀铁律**：
   - **表事先不存在（新建表+导数据）** ➔ **\`SELECT 列 INTO NewTable FROM 表1\`**（加 \`WHERE 1=0\` 仅拷结构）。
   - **表事先已存在（向已有表追加数据）** ➔ **\`INSERT INTO 已有表 SELECT ... FROM 表1\`**。
   - **口诀**：“新表用 SELECT INTO，老表用 INSERT INTO SELECT！”

39. **添加数据库文件语法秒杀铁律（ALTER DATABASE ... ADD FILE）**：
   - **核心结构**：\`ALTER DATABASE 库名 ADD FILE ( NAME = 逻辑名, FILENAME = '物理绝对路径', SIZE = 大小, FILEGROWTH = 增长率 )\`。
   - **考场四参数**：**NAME**（逻辑名）、**FILENAME**（单引号物理绝对路径）、**SIZE**（初始大小）、**FILEGROWTH**（增长幅度）。

40. **SELECT 基本语法格式与子句顺序秒杀铁律**：
   - **严格书写顺序**：\`SELECT ... FROM ... WHERE ... GROUP BY ... ORDER BY ...\`。
   - **秒杀口诀**：“**选自哪里分个序**”（SELECT ➔ FROM ➔ WHERE ➔ GROUP BY ➔ ORDER BY）。
   - **顺序陷阱**：\`WHERE\` 永远在 \`GROUP BY\` 之前；\`ORDER BY\` 永远位于整句最后！

41. **触发器三大时机关键字（AFTER / FOR / INSTEAD OF）秒杀铁律**：
   - **后触发器等价性**：**\`FOR\` 与 \`AFTER\` 完全等价**（执行完 SQL 后才触发，只能建在**基表**上）。
   - **替代触发器**：**\`INSTEAD OF\`**（跳过原 SQL 改执行自身，基表与**视图**均可建，常用于使视图可更新）。
   - **秒杀口诀**：“**FOR 即 AFTER 只依表，替代触发 INSTEAD OF 视图也能上！**”`
    }
  ]
};
