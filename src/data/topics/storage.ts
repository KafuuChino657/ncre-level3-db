import { Topic } from '../../types';

export const storageTopic: Topic = {
  id: 'storage-indexing',
  title: '物理设计、存储结构与索引技术',
  badge: '底层物理',
  icon: 'HardDrive',
  description: '聚集vs非聚集索引、稠密vs稀疏、反规范化技术、表分区黄金三部曲、常用RAID阵列全景计算(0/1/5/6/10)、物理文件与文件组(.mdf/.ndf/.ldf/PRIMARY)',
  subTopics: [
    {
      id: 'cluster-vs-noncluster',
      title: '1. 聚集索引 vs 非聚集索引对比与索引使用五大黄金原则',
      tag: '核心大考点',
      highlightedKeywords: [
        '聚集索引',
        '非聚集索引',
        '索引使用原则',
        '作为条件被使用',
        '排序或分组',
        '值域很大',
        '复合索引',
        '检查索引的完整性',
        '叶子节点',
        '物理顺序',
        '书签',
        'RID',
        '最多1个'
      ],
      content: `### 聚集索引与非聚集索引核心机理对比

在基于 B+ 树的数据库存储引擎（如 SQL Server、MySQL InnoDB）中，索引的物理结构差异如下：

| 对比维度 | 聚集索引 (Clustered Index) | 非聚集索引 (Non-clustered Index) |
| :--- | :--- | :--- |
| **物理存储顺序** | 索引键值的逻辑顺序与数据行的**实际物理存放顺序严格一致** | 索引中键值的逻辑顺序与物理存放顺序**无关** |
| **叶子节点内容** | 叶子节点就是**真实完整的数据行（包含整行所有列）** | 叶子节点只存放**索引键值 + 行定位符（书签/RID/聚集主键）** |
| **一张表的数量限制**| **每张数据表最多只能创建 1 个聚集索引**（因为物理顺序只有一种） | 一张表可以建立**多个非聚集索引**（如最多 249 或 999 个） |
| **范围查询性能** | **极高**（只需找到起点，顺着物理连续块顺序扫描即可） | 较低（非聚集索引需要回表读取实际数据行，随机 I/O 多） |
| **数据插入更新代价**| 较高（新插入记录可能引发“页拆分 Page Split”和重排） | 较低（数据行插在堆末尾，只更新索引树） |

#### 聚集索引适用列与不适用列原则：
- **适合建立聚集索引**：
  1. 经常需要按**范围查询（BETWEEN、>、<）**的列；
  2. 经常需要进行**分组（GROUP BY）**或**排序（ORDER BY）**的列；
  3. 查询结果返回大量连续记录的列。
- **不适合建立聚集索引**：
  1. 频繁被修改更新的列（会导致大量物理移动）；
  2. 取值重复率极高的列（如性别列，区分度低）；
  3. 很少出现在 WHERE 过滤子句中的列。

---

### 二、 索引使用的五大通用原则（教材官方考点・真题原句）

> 💡 **“索引的使用要恰到好处，其使用原则一般如下”** —— 考场高频简答与选择填空核心考点：

1. **经常在查询中作为条件被使用的列，应为其建立索引。**
   - **原理**：经常出现在 \`WHERE\` 条件子句中的列，建立索引后查询引擎可通过 B+ 树快速二分查找检索目标行，避免全表扫描（Table Scan）带来的高额磁盘 I/O。
2. **频繁进行排序或分组（即进行 group by 或 order by 操作）的列，应为其建立索引。**
   - **原理**：B+ 树索引中的键值天然是有序存储的。若在该列建有索引，执行 \`ORDER BY\` 或 \`GROUP BY\` 时可以直接利用索引的现有物理/逻辑顺序，消除内存或临时磁盘空间中极其耗费 CPU 资源的临时排序（Sort Operator）运算。
3. **一个列的值域很大时，应为其建立索引。**
   - **原理**：列的值域很大意味着**基数（Cardinality）高、取值离散度与区分度大**（如身份证号、手机号、唯一流水号等），索引过滤性极佳；反之，若值域极小（如性别只有男/女，重复度 50%），走索引还需要大量回表，性能反而远劣于顺序全表扫描。
4. **如果待排序的列有多个，应在这些列上建立复合索引。**
   - **原理**：当 SQL 查询包含多字段联合排序（如 \`ORDER BY ColA ASC, ColB DESC\`）或多条件组合过滤时，单列索引无法同时发挥全部效能，建立多字段联合的**复合索引（Composite / Composite Index）**可一次性匹配排序规则。
5. **可以使用系统工具来检查索引的完整性，必要时进行修复。**
   - **原理**：在高并发频繁增删改、意外宕机或断电后，索引的 B+ 树指针、页拆分链表可能产生碎片化甚至坏块损坏。DBA 应使用数据库管理工具或命令（如 SQL Server 的 \`DBCC CHECKTABLE\`、\`DBCC CHECKDB\`、\`ALTER INDEX ... REBUILD\` 等）定期检查索引的完整性并实施碎片整理与修复。`
    },
    {
      id: 'dense-vs-sparse',
      title: '2. 稠密索引 vs 稀疏索引（做题三步秒杀法）',
      tag: '图形题秒杀',
      highlightedKeywords: [
        '稠密索引',
        '稀疏索引',
        '每个记录',
        '每个物理块',
        '稀疏必定是聚集',
        '非聚集只能是稠密',
        '三步判断法'
      ],
      content: `### 稠密索引与稀疏索引读图题 3 步判断法

在三级考试中，经常给出两个结构框图（左边是索引项，右边是数据文件块），要求判断是稠密还是稀疏、聚集还是非聚集。牢记以下定义与逻辑铁律：

#### 1. 核心定义
- **稠密索引 (Dense Index)**：数据文件中的**每一个记录（每一行搜索码值）**在索引中都有一个对应的索引项。
- **稀疏索引 (Sparse Index)**：数据文件中只有**部分记录（通常是每个物理磁盘块的第一条记录，即块首锚记录）**在索引中有索引项。

#### 2. 做题三步判断法（万无一失）
1. **看箭头数量与对应关系**：
   - 如果索引列表里每一条数据记录都能找到对应的索引行 → **稠密索引**；
   - 如果一个数据块（Block）里有好多行数据，但只有一个索引项指向这个数据块的开头 → **稀疏索引**。
2. **看右侧数据文件是否有序**：
   - 如果数据文件本身是按索引键值严格有序排列的 → **聚集索引**；
   - 如果数据文件是无序存放的（乱序） → **非聚集索引**。
3. **套用考场终极互斥铁律**：
   - **铁律一：稀疏索引必定是聚集索引！**（因为数据文件如果不按该键有序，稀疏索引根本找不到目标记录）。
   - **铁律二：非聚集索引绝不可能是稀疏索引，非聚集索引只能是稠密索引！**`
    },
    {
      id: 'file-organization',
      title: '3. 常见文件组织方式（堆/顺序/散列）',
      tag: '物理存储',
      highlightedKeywords: ['堆文件', '顺序文件', '散列文件', '哈希冲突', '溢出桶'],
      content: `### 文件组织方式与性能特性

物理设计阶段需要为表选择合适的文件组织结构：

| 文件组织类型 | 物理存放特征 | 优点 | 缺点 / 适用场景 |
| :--- | :--- | :--- | :--- |
| **堆文件 (Heap)** | 数据记录无序追加存放在文件末尾，不预先排序 | 插入记录速度极快（O(1) 复杂度） | 范围查询和点查询极慢，必须全表扫描 |
| **顺序文件 (Sequential)** | 记录按某个搜索码字段的值排序物理存储 | 顺序扫描和范围查询速度极快 | 插入和删除记录需要维护物理顺序，容易产生空间碎片 |
| **散列文件 (Hash)** | 利用哈希函数 h(k) 将记录映射到固定的桶（Bucket）地址 | 基于等值条件的点查询速度极快（常数级 O(1)） | **完全不支持范围查询（>、<）**，哈希冲突需挂载溢出桶 |
| **B/B+ 树文件** | 平衡多路查找树，叶子节点构成双向链表 | 点查、范围查、排序性能均非常优秀且稳定 | 占用额外索引空间，高频写操作需要维护树分裂 |`
    },
    {
      id: 'denormalization',
      title: '4. 数据库反规范化（去规范化）技术',
      tag: '性能调优',
      highlightedKeywords: [
        '反规范化',
        '增加冗余列',
        '增加派生列',
        '水平分割',
        '垂直分割',
        'JOIN',
        'UNION',
        '数据不一致'
      ],
      content: `### 为什么需要反规范化（Denormalization）？

规范化理论（3NF/BCNF）虽然消除了插入、删除异常和数据冗余，但**过度规范化会导致多表 JOIN 连接操作过多，严重降低系统读性能**。为了提高高并发下的查询性能，物理设计常采用反规范化技术。

#### 常用反规范化手段：
1. **增加冗余列**：在经常需要联合查询的表中，将另一张表的高频展示字段冗余存放（如在订单表中直接增加客户姓名字段，避免每次都要关联客户表）。
2. **增加派生列/统计列**：在主表中存放由其他字段聚合计算出的值（如在班级表中增加“学生总人数”字段，避免每次查询都执行 \`COUNT(*)\`）。
3. **表垂直分割（Vertical Partitioning）**：
   - 将主键与高频访问列拆分为一张主表；
   - 将低频访问列、大字段（如 BLOB、TEXT 详细描述）拆分到扩展表中；
   - 减少主表每一行宽度，增加每页缓存记录数，大幅提升 I/O 吞吐。
   - **核心考点与查询特征**：垂直分割后，若要查询某实体的**所有数据（完整属性）**，必须使用 **JOIN（连接）** 操作将各分割表关联在一起。
4. **表水平分割（Horizontal Partitioning）**：
   - 将一张超大表的数据行按时间（如按年份、月份）、地区或哈希取模拆分成若干张结构完全相同的子表；
   - 单表数据量减小，索引树层级降低。
   - **核心考点与查询特征**：水平分割后，若要查询跨越各子表的**所有数据（全量记录）**，必须使用 **UNION / UNION ALL（并集）** 操作将各个子表结果合并。

#### 反规范化的代价（做题提醒）：
- **存储空间开销增大**；
- **数据更新代价增加**（修改一个值必须同步更新冗余列）；
- **存在数据不一致性风险**（必须使用事务、触发器或定时批处理任务来保证冗余数据同步）。`
    },
    {
      id: 'partitioned-tables',
      title: '5. 表分区技术（Partitioned Tables）黄金三部曲与核心陷阱',
      tag: '物理设计必考',
      highlightedKeywords: [
        '表分区',
        '水平划分',
        '分区函数',
        '分区方案',
        '分区表',
        '分区消除',
        'RANGE LEFT',
        'RANGE RIGHT',
        'N+1个分区'
      ],
      content: `### 表分区（Partitioned Tables）核心考点精讲

在全国计算机等级考试三级数据库技术中，**表分区（Partitioned Tables）** 是物理数据库设计与海量数据管理的高频考点，常以选择题、综合判断题和填空题形式出现。

---

### 一、 核心概念与本质（考判断与选择）

1. **水平划分而非垂直划分**：
   - 表分区本质上属于**水平划分（Horizontal Partitioning）**。
   - 它将一张大表中的**数据行（Record / Row）**按照某种业务规则（如按年份、月份、销售大区）分散切分到不同的物理子区域中，而**所有的列结构（Schema / 表结构）完全保持一致**。
   - *（干扰项常设为“垂直划分”，注意垂直划分是将不同字段/列拆开，切勿混淆！）*

2. **逻辑一体，物理分离**：
   - **对开发人员与应用程序完全透明**：在应用层和 SQL 层面看来，它仍然是**一张普通的单一逻辑表**（执行 \`SELECT * FROM Orders\` 毫无感知，无需指定特定子表名）。
   - **底层物理存储严格隔离**：在数据库底层存储引擎中，这些数据行被分别保存在不同的物理文件（Files）或文件组（Filegroups）中。

3. **核心应用目的**：
   - **大幅提升查询性能**：支持**分区消除（Partition Elimination / Pruning，也称分区修剪）**。例如执行 \`WHERE OrderDate >= '2025-01-01'\`，查询优化器会自动跳过其他所有年份无关分区，**仅扫描目标物理分区**，大幅减少磁盘 I/O。
   - **提升系统可用性与可维护性**：备份、恢复、数据归档或重建索引均可以**针对单个分区独立进行**，无须锁死整张千万级超大表。

---

### 二、 创建表分区的“黄金三部曲”（必考先后顺序与语法）

在 SQL Server 物理设计中，创建分区表必须按照**严格的先后顺序**执行以下 3 个步骤（顺序绝对不可颠倒）：

\`\`\`text
步骤 1：创建分区函数 (Partition Function) —— 定义“怎么切”（切分规则与边界值）
             ↓
步骤 2：创建分区方案 (Partition Scheme) —— 定义“切好的块放哪儿”（映射物理文件组）
             ↓
步骤 3：创建分区表 (Partition Table) —— 实际建表应用（绑定分区方案与分区依据列）
\`\`\`

#### 步骤 1：创建分区函数（定义怎么切）
- **功能作用**：定义按照什么数据类型切分，以及切分的**边界值（Boundary Values）**。

\`\`\`sql
-- 步骤 1：创建分区函数（定义分区列数据类型与边界切分点）
CREATE PARTITION FUNCTION pf_OrderDate (datetime)                    -- 第1句：定义创建名为 pf_OrderDate 的分区函数，指定分区列数据类型为 datetime
AS RANGE RIGHT                                                       -- 第2句：指定边界归属方式为 RANGE RIGHT，边界值自身划入右侧（大于等于）分区
FOR VALUES ('2024-01-01', '2025-01-01', '2026-01-01');              -- 第3句：显式声明3个划分边界切分点，将在底层生成 3+1 = 4 个逻辑分区
\`\`\`

##### 【逐句代码详解】
- **\`CREATE PARTITION FUNCTION pf_OrderDate (datetime)\`**：定义分区函数对象。分区函数只负责定义**“切分规则与边界值”**，明确分区依据列的数据类型（必须与后续表对应列兼容），此时尚未与具体物理文件组或数据表挂钩。
- **\`AS RANGE RIGHT\`**：划分边界方向关键字。
  - **\`RANGE RIGHT\`**：边界值归属于右侧分区（即满足 \`x >= 边界值\`）；
  - **\`RANGE LEFT\`**：边界值归属于左侧分区（即满足 \`x <= 边界值\`）。
- **\`FOR VALUES ('2024-01-01', '2025-01-01', '2026-01-01')\`**：指定切割的边界值集合。
  - **考场核心计算铁律**：**N 个边界值必定切出 N + 1 个分区**！
  - 此处声明了 3 个边界时间点，因此会划分出 **4 个逻辑分区范围**：
    - 分区 1：\`(-∞, 2024-01-01)\`
    - 分区 2：\`[2024-01-01, 2025-01-01)\`
    - 分区 3：\`[2025-01-01, 2026-01-01)\`
    - 分区 4：\`[2026-01-01, +∞)\`

---

#### 步骤 2：创建分区方案（定义切好的块放哪儿）
- **功能作用**：将步骤 1 中切出的逻辑分区，映射到物理存储的**文件组（Filegroup）**上。

\`\`\`sql
-- 步骤 2：创建分区方案（将逻辑分区映射至底层物理文件组）
CREATE PARTITION SCHEME ps_OrderDate                                 -- 第1句：定义创建名为 ps_OrderDate 的分区方案对象
AS PARTITION pf_OrderDate                                            -- 第2句：关联绑定此前已创建的分区函数 pf_OrderDate
TO (fg1, fg2, fg3, fg4);                                             -- 第3句：按顺序将 4 个逻辑分区分别映射到物理存储文件组 fg1~fg4
\`\`\`

##### 【逐句代码详解】
- **\`CREATE PARTITION SCHEME ps_OrderDate\`**：定义物理分区方案。分区方案的作用是**作为逻辑分区函数与物理文件组之间的“映射桥梁”**。
- **\`AS PARTITION pf_OrderDate\`**：指定本方案依据哪一个分区函数的切分规则进行映射。
- **\`TO (fg1, fg2, fg3, fg4)\`**：指定物理存放的目标文件组列表。
  - **高频考点陷阱 1**：文件组数量必须与分区数量严格匹配（4 个分区对应 4 个文件组名称），或者使用 \`TO (ALL [PRIMARY])\` 全部映射到默认主文件组；
  - **高频考点陷阱 2（单选极高频）**：各分区**完全不需要、也不强制映射到不同的文件组**！多个分区完全可以映射到同一个文件组，甚至全部映射在 \`PRIMARY\` 主文件组中。

---

#### 步骤 3：创建分区表（实际应用建表）
- **功能作用**：用常规建表语句建立表模式，但在结尾不写普通的文件组，而是**指定分区方案和分区依据列**。

\`\`\`sql
-- 步骤 3：创建分区表（实际建表并绑定分区方案与分区列）
CREATE TABLE Orders (                                                -- 第1句：定义创建名为 Orders 的新业务数据表
    OrderID INT NOT NULL,                                            -- 第2句：订单编号整型非空
    OrderDate DATETIME NOT NULL,                                     -- 第3句：下单时间日期类型非空，作为分区依据列
    TotalAmount DECIMAL(10, 2),                                      -- 第4句：订单金额列
    CONSTRAINT PK_Orders PRIMARY KEY (OrderID, OrderDate)            -- 第5句：【高频考点】若建主键，分区列 OrderDate 必须包含在主键属性列表中
) ON ps_OrderDate (OrderDate);                                       -- 第6句：【核心子句】用 ON 指定分区方案 ps_OrderDate，并传入分区列 OrderDate
\`\`\`

##### 【逐句代码详解】
- **\`CREATE TABLE Orders (...)\`**：标准 DDL 建表语句，定义表的字段模式。
- **\`CONSTRAINT PK_Orders PRIMARY KEY (OrderID, OrderDate)\`**：
  - **考试第一大陷阱**：如果分区表包含主键（PRIMARY KEY）或唯一约束（UNIQUE），**分区依据列（如 OrderDate）必须包含在主键或唯一约束的列清单中**！
  - **原因**：因为数据库引擎必须在各个物理分区内独立保证记录的唯一性，只有将分区列纳入主键，才能构建**对齐局部索引（Aligned Index）**，否则系统会直接报错拒绝建表。
- **\`ON ps_OrderDate (OrderDate)\`**：分区表声明核心句。普通建表末尾是 \`ON [PRIMARY]\`，而分区表必须写为 **\`ON 分区方案名 (分区依据列)\`**，通知存储引擎在写入数据行时根据 OrderDate 自动计算所属分区并写入对应物理文件组。

---

### 三、 考场高频真题考点对比表（必背考眼）

| 考察维度 | 正确表述（高频正确选项） | 常见干扰项（必错陷阱） | 考点命题解析 |
| :--- | :--- | :--- | :--- |
| **切分方式** | **水平划分（按行切分）** | 垂直划分（拆分字段/列） | 表分区所有子分区的列结构（Schema）完全一致，仅行分散 |
| **创建首要步骤** | **第一步必须先建分区函数** | 第一步先建分区表或分区方案 | 黄金三部曲顺序：分区函数 → 分区方案 → 分区表 |
| **分区与文件组映射**| 可以映射到**同一个**文件组，也可映射到不同文件组 | **必须**映射到不同的物理文件组 | 绝对化陷阱！所有分区完全可以同时放在 PRIMARY 文件组中 |
| **边界值计算** | **N 个边界值生成 N + 1 个分区** | N 个边界值生成 N 个分区 | 3 个切分时间点切出 4 段区间 |
| **分区列与主键约束**| 分区依据列**必须是主键/唯一键的一部分** | 分区列不能出现在主键中 | 强行要求对齐，若未包含分区列建主键将直接报错 |
| **应用透明性** | **对应用程序和开发人员透明**（逻辑单一表） | 开发人员查询时必须指定具体子分区 | 执行普通 \`SELECT * FROM 表\` 即可，底层自动做分区消除 |`
    },
    {
      id: 'raid-storage-arrays',
      title: '6. 常用 RAID 阵列全景对比与计算（RAID 0/1/5/6/10 必背公式）',
      tag: '底层存储与高可用',
      highlightedKeywords: [
        'RAID 0',
        'RAID 1',
        'RAID 5',
        'RAID 6',
        'RAID 10',
        '条带化',
        '镜像',
        '奇偶校验分布式条带',
        '双重奇偶校验',
        '可用容量公式',
        '(N-1)×C',
        '(N-2)×C',
        '(N/2)×C',
        '写惩罚',
        '最少盘数'
      ],
      content: `### 常用 RAID 阵列全景对比表（必背计算公式）

> 💡 **考场计算基准设定**：
> 假设共有 **$N$** 块单盘容量为 **$C$** 的硬盘（$N$ 满足该阵列的最低盘数要求）：

| RAID 级别 | 中文名称 | 最少盘数 | 可用容量公式 | 容错/冗余能力（最多允许坏几块） | 读/写性能特征 | 适用数据库场景 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **RAID 0** | **条带化（Striping）** | **2** | **$N \\times C$**（100%） | **0 块**（无冗余，坏一块全完） | 读写极快（吞吐量翻倍） | 临时库、日志暂存、无安全要求的测试环境 |
| **RAID 1** | **镜像（Mirroring）** | **2**（必须是偶数） | **$1 \\times C$**（以2块盘为例，利用率 50%） | **1 块**（坏掉任意一块不丢数据） | 读性能翻倍，写性能与单盘相当 | 存放关键的事务日志文件（Log Files） |
| **RAID 5** | **奇偶校验分布式条带** | **3** | **$(N - 1) \\times C$** | **1 块**（坏掉任意一块不丢数据） | 读性能好；写性能有“写惩罚”（需算校验） | 存放读多写少的数据文件、数据仓库（OLAP） |
| **RAID 6** | **双重奇偶校验** | **4** | **$(N - 2) \\times C$** | **2 块**（坏掉任意两块不丢数据） | 读性能好；写性能进一步下降 | 大容量冷数据归档、数据仓库 |
| **RAID 10 (1+0)** | **先镜像后条带** | **4**（必须是偶数） | **$(N / 2) \\times C$**（固定 50%） | 每组镜像可坏 1 块（极限可坏 N/2 块，绝不丢数据） | 读写俱佳，无写惩罚，性能顶级 | 核心 OLTP 业务数据库主力配置（高并发读写） |

---

### 一、 五大主流 RAID 架构深度剖析

#### 1. RAID 0（条带化 Striping —— 纯速度，零冗余）
- **实现原理**：将连续数据切分成多个数据块（Block），并行交错存放在各个硬盘上。
- **盘数与容量**：最少 **2 块盘**；可用空间为 $N \\times C$（利用率 100%）。
- **容错能力**：**完全没有冗余校验能力（0 块容错）**。只要其中任何 1 块硬盘发生故障，整组阵列的数据将全部损坏崩溃！
- **优缺点与应用**：读写并发吞吐极高，但无安全性，仅适合开发测试、临时数据计算、只读缓存。

#### 2. RAID 1（镜像 Mirroring —— 极致安全，容量折半）
- **实现原理**：数据在写入工作盘的同时，自动向镜像盘写入完全一致的一份镜像副本。
- **盘数与容量**：最少 **2 块盘**（盘数必须为偶数）；2 块盘时可用空间仅为 $1 \\times C$（利用率恒为 50%）。
- **容错能力**：**允许坏 1 块盘**。只要镜像对中有一块存活，数据绝不丢失。
- **优缺点与应用**：写操作需写双份，但读操作可从两盘并行读取提高读效率。常用于**存放数据库重做日志文件（Transaction Log Files）**或操作系统根分区。

#### 3. RAID 5（分布式奇偶校验条带 —— 平衡之王，兼顾成本与安全）
- **实现原理**：数据以条带形式分布存储在不同磁盘上，奇偶校验信息（Parity）**均匀分散交叉存储在所有磁盘**中，没有单独的专用校验盘。
- **盘数与容量**：最少 **3 块盘**；可用容量为 **$(N - 1) \\times C$**（相当于拿出 1 块盘的总容量用于存储校验码）。
- **容错能力**：**最多允许坏 1 块盘**。坏掉一块后，可利用剩余磁盘的数据与校验信息异或重建。
- **写惩罚（Write Penalty）**：每次写入/更新数据时，必须经历“读旧数据、读旧校验、算新校验、写新数据、写新校验”的4次 I/O 过程，导致写性能存在折扣。适合**读多写少（OLAP）的普通数据文件**。

#### 4. RAID 6（双重分布式奇偶校验 —— 极高容灾，允许双盘齐崩）
- **实现原理**：在 RAID 5 基础上引入了两套独立的异或/Reed-Solomon 奇偶校验算法（P + Q），校验信息同样分布式交错存放。
- **盘数与容量**：最少 **4 块盘**；可用容量为 **$(N - 2) \\times C$**（相当于消耗 2 块盘的校验开销）。
- **容错能力**：**最多允许损坏 2 块硬盘同时掉线**而不丢失数据！
- **应用场景**：超大容量硬盘在重构期间再次损坏概率增加时使用，多见于关键冷数据存储和大型数据仓库。

#### 5. RAID 10 (RAID 1+0，先做镜像，再做条带化 —— 顶级性能与企业主力)
- **实现原理**：先两两配对构建 RAID 1 镜像保护卷，再将各个镜像卷跨盘组合成 RAID 0 条带化加速阵列。
- **盘数与容量**：最少 **4 块盘**（盘数必须为偶数）；可用容量为 **$(N / 2) \\times C$**（空间利用率固定为 50%）。
- **容错能力**：在同一镜像对不同时损坏的前提下，最多允许坏 $N / 2$ 块盘；无论如何必保**坏任意 1 块盘不丢数据**。
- **应用场景**：兼具 RAID 1 的绝对安全与 RAID 0 的并发高性能，且没有 RAID 5 的写惩罚，是**大型企业级 OLTP 高并发数据库（如订单库、金融交易库的数据与日志混合区）的工业标准首选**！

---

### 二、 考场经典容量计算真题实战

1. **题目 1**：某企业数据库服务器配备了 5 块容量为 2TB 的硬盘组建 **RAID 5** 阵列，则该阵列对操作系统呈现的可用存储容量是？
   - **解析**：代入公式 $(N - 1) \\times C = (5 - 1) \\times 2\\text{TB} = \\mathbf{8\\text{TB}}$。
2. **题目 2**：若使用 6 块容量为 4TB 的硬盘组建 **RAID 6** 阵列，可用容量为？最多允许同时损坏几块盘？
   - **解析**：
     - 可用容量：$(N - 2) \\times C = (6 - 2) \\times 4\\text{TB} = \\mathbf{16\\text{TB}}$。
     - 允许损坏盘数：RAID 6 拥有双重奇偶校验，**最多允许坏 2 块盘**。
3. **题目 3**：8 块 1TB 磁盘组建 **RAID 10** 阵列，可用容量是？
   - **解析**：代入公式 $(N / 2) \\times C = (8 / 2) \\times 1\\text{TB} = \\mathbf{4\\text{TB}}$。

---

### 三、 考场辨析与选择填空直通避坑口诀

- **最少盘数口诀**：
  - RAID 0 最少 **2 块**；RAID 1 最少 **2 块**；RAID 5 最少 **3 块**；RAID 6 最少 **4 块**；RAID 10 最少 **4 块**。
- **容错盘数口诀**：
  - RAID 0 坏 **0 块**（坏一不可用）；
  - RAID 1 和 RAID 5 均只允许坏 **1 块**；
  - RAID 6 允许坏 **2 块**；
  - RAID 10 每组镜像可坏 1 块（极限坏 $N/2$ 块）。
- **场景秒选口诀**：
  - “**存放关键事务日志**” ➔ 秒选 **RAID 1**！
  - “**核心高并发 OLTP 业务数据库主力**” ➔ 秒选 **RAID 10**！
  - “**读多写少、性价比高的数据仓库/数据文件**” ➔ 秒选 **RAID 5**！`
    },
    {
      id: 'files-and-filegroups',
      title: '7. 数据库物理文件与文件组体系（.mdf / .ndf / .ldf 与主文件组 PRIMARY 考点精讲）',
      tag: '物理文件结构',
      highlightedKeywords: [
        '主要数据文件',
        'Primary Data File',
        '.mdf',
        '次要数据文件',
        'Secondary Data File',
        '.ndf',
        '事务日志文件',
        'Transaction Log File',
        '.ldf',
        '文件组',
        'Filegroup',
        '主文件组',
        'PRIMARY',
        '日志文件不属于任何文件组',
        'ALTER DATABASE',
        'ADD FILE'
      ],
      content: `### 核心概念：文件与文件组的关系精讲

在 SQL Server 等现代关系型数据库中，数据库在底层的物理存储是由**物理文件**和**逻辑文件组**共同构建的：

---

### 一、 三类数据库文件（物理存储基石）

| 文件类别 | 英文全称与扩展名 | 数量要求 | 核心职能与存储内容 | 考场易错陷阱 |
| :--- | :--- | :--- | :--- | :--- |
| **主要数据文件** | **Primary Data File**<br>(\`.mdf\`) | **有且仅有 1 个** | 存放数据库的**启动信息**与**系统元数据表**，是数据库物理存在的唯一根基。 | 绝不能有多个，且**必须归属于主文件组（PRIMARY）**！ |
| **次要数据文件** | **Secondary Data File**<br>(\`.ndf\`) | **0 个到多个**（可选） | 存放主要数据文件容纳不下的用户数据，用于**分散存储**与跨磁盘**负载均衡**（可分布在不同物理驱动器上）。 | 可以没有（小库只需一个 .mdf 即可）。如果不指定文件组，默认进入主文件组。 |
| **事务日志文件** | **Transaction Log File**<br>(\`.ldf\`) | **至少 1 个**（可有多个） | 记录事务对数据库的所有更新操作及日志恢复信息，用于故障恢复（UNDO / REDO）。 | **【最高频题眼】事务日志文件绝不属于任何文件组！** |

---

### 二、 文件组（Filegroup）机制与核心规则

文件组是 SQL Server 将多个物理数据文件在逻辑上汇聚成一个命名管理单元的机制：

1. **主文件组（PRIMARY Filegroup）**：
   - 系统在执行 \`CREATE DATABASE\` 创建数据库时**自动建立的默认文件组**。
   - 包含系统的系统表、系统元数据视图及系统内部结构。

2. **核心四大分配与放置规则（做题必背铁律）**：
   - 规则 1：**主要数据文件（\`.mdf\`）必须且只能建立在主文件组（PRIMARY）中**，绝不能挪到次要用户文件组！
   - 规则 2：如果用户建库时**没有显式创建自定义用户文件组**，那么后续新建的次要数据文件（\`.ndf\`）**也会默认归入主文件组（PRIMARY）**。
   - 规则 3：**事务日志文件（\`.ldf\`）与文件组完全解耦**，日志文件只管理日志记录流，**绝不能、也绝不会属于任何文件组**！
   - 规则 4：数据文件只能且仅能属于一个文件组，**不能跨文件组共享同一个物理数据文件**。

---

### 三、 添加数据库文件的语法格式（ALTER DATABASE ... ADD FILE）

当数据库容量不足或需要扩展次要数据文件时，添加文件的标准 T-SQL 语法格式如下：

\`\`\`sql
ALTER DATABASE 要添加文件的数据库 ADD FILE (
    NAME = 逻辑文件名,
    FILENAME = '文件所在的绝对路径',
    SIZE = 初始文件大小,
    FILEGROWTH = 增长速率
);
\`\`\`

#### 极简参数速记：
- **\`ALTER DATABASE 数据库名 ADD FILE (...)\`**：固定开头，表明修改目标数据库并添加文件。
- **\`NAME\`**：逻辑文件名（数据库内部引用的名称）。
- **\`FILENAME\`**：操作系统层面的**绝对物理路径**（如 \`'D:\\Data\\mydb_data2.ndf'\`，必须带单引号）。
- **\`SIZE\`**：文件的初始大小（如 \`10MB\`）。
- **\`FILEGROWTH\`**：自动增长幅度（如 \`5MB\` 或 \`10%\`）。

---

### 四、 考场避坑直选速查清单

- 题干问：“**每个数据库中有且仅能有几个主要数据文件？扩展名是什么？**”
  ➔ 秒选 **1 个**，扩展名为 **\`.mdf\`**。
- 题干问：“**次要数据文件的扩展名与数量限制是？**”
  ➔ 秒选 **\`.ndf\`**，数量为 **0 到多个**。
- 题干问：“**事务日志文件的扩展名与所属文件组是？**”
  ➔ 秒选 扩展名为 **\`.ldf\`**，**不属于任何文件组**！
- 题干问：“**主要数据文件（.mdf）必须保存在哪个文件组中？**”
  ➔ 秒选 **主文件组（PRIMARY）**！`
    },
    {
      id: 'storage-manager-architecture',
      title: '7. 存储管理器结构与查询处理器对比',
      tag: '高频架构',
      highlightedKeywords: [
        '存储管理器',
        '缓冲区管理器',
        '文件管理器',
        '权限及完整性管理器',
        '事务管理器',
        '日志与恢复管理器',
        '查询处理器',
        '执行计划',
        'WAL协议'
      ],
      content: `### 存储管理器 (Storage Manager) 核心职责与架构

存储管理器是 DBMS 中负责在**底层物理存储设备（磁盘）**与**内存/上层执行引擎**之间进行数据交互的核心子系统。其根本目标是在保证事务 ACID 特性与数据安全的前提下，尽量减少昂贵的磁盘 I/O 开销。

---

#### 一、存储管理器的核心子模块

| 子模块 | 核心职责 | 关键考点/机制 |
| :--- | :--- | :--- |
| **缓冲区管理器 (Buffer Manager)** | 将物理磁盘的数据页 (Page/Block) 读取并缓存在内存的“缓冲池 (Buffer Pool)”中 | 空间不足时，采用 **LRU / Clock** 等页面置换算法将脏页刷盘并换出内存 |
| **文件管理器 (File Manager)** | 负责与操作系统底层文件系统交互，管理磁盘空间的分配、回收与回收块重用 | 负责数据文件、索引文件和日志文件的物理扩展与分配 |
| **权限及完整性管理器 (Authorization & Integrity Manager)** | 在真正读写底层数据时，校验请求用户的访问权限与完整性约束 | 权限校验 (SELECT/UPDATE)；完整性校验（主键唯一性、外键引用、Check 约束） |
| **事务管理器 (Transaction Manager)** | 负责管理并发事务对数据的访问，控制并发读写 | 通过加锁 (S锁/X锁) 或多版本并发控制 (MVCC) 保证事务的**隔离性 (I) 与一致性 (C)** |
| **日志与恢复管理器 (Recovery Manager)** | 负责维护事务日志与故障恢复 | 遵循 **WAL 协议（Write-Ahead Logging，日志先行）**；故障后通过 REDO（重做）和 UNDO（回滚）保证**原子性 (A) 与持久性 (D)** |

---

#### 二、存储管理器 vs 查询处理器（职责辨析考点）

| 对比维度 | 查询处理器 (Query Processor) | 存储管理器 (Storage Manager) |
| :--- | :--- | :--- |
| **主要定位** | 上层脑力：“负责思考与规划” | 底层体力：“负责跑腿与存取” |
| **核心职责** | 词法/语法分析、权限预审、关系代数逻辑优化、**生成物理执行计划 (Execution Plan)** | 依照执行计划具体取数、管理内存缓冲块、底层读写锁控制、日志落盘、物理文件管理 |
| **处理对象** | SQL 文本、语法树、关系代数表达式 | 内存数据页、磁盘块、事务锁、日志文件 |

---

#### 🎯 考场秒杀题眼
- **问职责辨析**：凡是“编译、语法分析、查询优化、生成执行计划” ➔ 归【**查询处理器**】；凡是“缓冲池管理、数据页刷盘、事务加锁、日志恢复、文件分配” ➔ 归【**存储管理器**】。
- **问子模块**：
  - “负责将数据页从磁盘读入缓冲池，并在内存不足时换出脏页的是” ➔ 【**缓冲区管理器**】。
  - “遵循 WAL 协议，负责 REDO 和 UNDO 保证原子性持久性的是” ➔ 【**日志与恢复管理器**】。
  - “负责通过加锁或 MVCC 保证事务隔离性一致性的是” ➔ 【**事务管理器**】。`
    }
  ]
};
